# 更新日志

## 2.0.0 · 2026-09-12

- 全面重写 13 篇中文知识讲解，补齐具体推演、适用条件、常见错误及 39 个三语言运行示例。
- 按每篇文章的练习目的关联题目；做题页也优先使用明确关联，避免宽泛标签导致推荐不相关的算法。
- 新增 OpenCode Go 订阅适配，可复用本机 OpenCode 凭据，提供判题后的代码评审入口，评审固定使用当次提交和测试结果。
- Node 本地服务自动保存 `progress/learning.json`，支持旧浏览器数据迁移、版本冲突保护、原子写入及恢复时保留 API Key。
- 启动脚本优先使用 Node，自动选择空闲端口；保留 Python 静态模式。
- 增加进度读写、Go 转发、知识关联与示例输出测试。

## v1.1.0（2026-07-21）

### 新增：C++ 语言支持（跟 Python / JavaScript 表现一致）

- 题目页和知识页的语言切换加了 C++；编辑器自带 C++ 语法高亮（Monaco 自带，离线可用）。
- C++ 判题走免费的公共远程编译服务（Judge0，连不上就自动回退 Wandbox），**需要联网**；JavaScript / Python 判题还是完全离线。
- 核心代码 / ACM 双模式都支持 C++：普通函数、链表、二叉树、带环链表、随机链表、设计题（类调用）、原地修改题全覆盖；编译错误的行号能定位到编辑器里。
- 100 道题补齐了 C++ 模板和参考题解（核心 + ACM 共 400 段）。
- 13 篇知识文章补齐了 C++ 讲解代码块和可运行示例（输出跟 Python 版逐字节一致）。
- 校验器升级：100 题六套参考代码（2 模式 × 3 语言）本地全量编译校验；知识示例强制三语言齐全；本机没有 g++ 时 C++ 校验自动跳过并告警。

### 新增：左侧跳转目录

- 刷题页：全部 100 题的目录，排版跟随题库列表页视图偏好（按难度 / 平铺 / 按分类）。
- 知识页：全部 13 篇文章的目录。
- 分组可以折叠，状态会记住；打开目录会自动展开当前所在组并滚动定位到当前项。
- 点击条目跳转后会自动收起；点击面板外任意处或按 Esc 也能收起；悬浮设计不影响页面排版。

### 优化：知识详情页布局

- 正文改成单列居中并加宽（72ch → 84ch），宽屏右侧不再空一大片。
- 「关联题目」从右侧边栏移到正文末尾，改成卡片网格展示；「上一篇 / 下一篇」导航放在它后面。

### 修复

- 知识文章代码块语言嗅探：以 `// C++` 等注释开头的代码块不再误判成 JavaScript（之前选 C++ 时这些块会被错误隐藏）。
- 知识文章里「两个版本 / 两种语言」这类过时的表述，全部更新为三语言口径。
- C++ 判题进程中途崩溃时，已完成用例的结果不会再丢失。
- 远程判题服务返回异常时的错误提示更清楚。
