# 更新日志

## v1.1.0（2026-07-21）

### 新增：C++ 语言支持（与 Python / JavaScript 效果一致）

- 题目页与知识页语言切换新增 C++；编辑器 C++ 语法高亮（Monaco 自带，离线可用）。
- C++ 判题走免费公共远程编译服务（Judge0，不可达时自动回退 Wandbox），**需联网**；JavaScript / Python 判题仍完全离线。
- 核心代码 / ACM 双模式均支持 C++：普通函数、链表、二叉树、带环链表、随机链表、设计题（类调用）、原地修改题全覆盖；编译错误行号可定位到编辑器。
- 100 道题补齐 C++ 模板与参考题解（核心 + ACM 共 400 段）。
- 13 篇知识文章补齐 C++ 讲解代码块与可运行示例（输出与 Python 版逐字节一致）。
- 校验器升级：100 题六套参考代码（2 模式 × 3 语言）本地全量编译校验；知识示例强制三语言齐全；本机无 g++ 时 C++ 校验自动跳过并告警。

### 新增：左侧跳转目录

- 刷题页：全部 100 题目录，排版跟随题库列表页视图偏好（按难度 / 平铺 / 按分类）。
- 知识页：全部 13 篇文章目录。
- 分组可折叠且状态记忆；打开目录自动展开当前所在组并滚动定位到当前项。
- 点击条目跳转并自动收起；点击面板外任意处或按 Esc 收起；悬浮设计不影响页面排版。

### 优化：知识详情页布局

- 正文改为单列居中并加宽（72ch → 84ch），消除宽屏右侧大片空白。
- 「关联题目」从右侧边栏移至正文末尾，改为卡片网格展示；「上一篇 / 下一篇」导航随其后。

### 修复

- 知识文章代码块语言嗅探：以 `// C++` 等注释开头的代码块不再误判为 JavaScript（此前选 C++ 时这些块被错误隐藏）。
- 知识文章中「两个版本 / 两种语言」等过时表述全部更新为三语言口径。
- C++ 判题进程中途崩溃时，已完成用例的结果不再丢失。
- 远程判题服务返回异常时的错误提示更准确。
