// 知识文章：数组与哈希表
// 数据格式约定（所有知识文章遵循）：
// - slug: 英文短横线命名，同时是文件名与路由
// - tags: 与题库中题目的 tags 匹配，用于自动关联题目
// - relatedProblems（可选）: 显式指定关联题目 id，优先于 tags 匹配
// - content: Markdown 子集（### 标题、- 列表、``` 代码块、`行内代码`）
export default {
  slug: 'array-hashmap',
  title: '数组与哈希表',
  intro: '数组随机访问与哈希表 O(1) 查找，空间换时间的两大基石',
  tags: ['数组', '哈希表', '前缀和', '计数'],
  relatedProblems: [1, 41, 49, 128, 238, 438, 560],

  content: `
> 提示：建议先读《时间复杂度入门》，知道 O(1) 和 O(n) 的区别；会用「数组[下标]」取元素，就能看懂本篇。

去超市存包，柜子会给你一张号码牌。取包时报号，工作人员直接开那一格，不会把一百个格子挨个翻一遍。哈希表就是这张「号码牌 → 柜格」的对应关系：不看里面装了什么，按号直达。

### 是什么

先抛开术语，讲两种最基础的「存数据方式」。

**数组**：一排连号的格子。每个格子有固定编号（下标，从 0 开始），格子在内存里一个挨一个放，所以知道编号就能一步算出它在哪——取 \`nums[3]\` 不用从 0 开始数，这叫 **O(1) 随机访问**。

**哈希表**：一本「键 → 值」的登记簿。你把键（key，比如数字 7）交给它，它内部换算一下，直接翻到记着这个键的那一页。查找、登记、划掉平均都是 **O(1)**——这就是「报号码牌直接开柜」。

它凭什么一步定位？内部有一个**哈希函数**，负责把 key 换算成页码（类似把号码牌换算成柜号）。换算只要一次计算，所以不用一页页翻。代价是得多备一本簿子——这也是哈希表「空间换时间」称号的由来。

术语对照表：

| 大白话 | 正式叫法 | JavaScript | Python |
| --- | --- | --- | --- |
| 连号格子排 | 数组（array） | \`[1, 2, 3]\` | \`[1, 2, 3]\` |
| 键值登记簿 | 哈希表（hash map） | \`Map\` | \`dict\` |
| 只记「有没有」的簿子 | 哈希集合（hash set） | \`Set\` | \`set\` |

> 注意：哈希表的查找、插入、删除是**平均 O(1)**，发生大量碰撞时最坏可退化到 O(n)；刷题通常按平均 O(1) 估算。

两种结构怎么分工：

- 要**按位置**取元素、按顺序处理数据、下标本身就携带信息 → 用数组
- 要快速回答「某个值**在不在**、**在哪**、**出现过几次**」→ 用哈希表
- JS 的 Array 与 Python 的 list 都是动态数组：按下标访问 O(1)，尾部追加是均摊 O(1)，中间插入 / 删除要搬移后续元素，是 O(n)
- JS 的 Map 和现代 Python 的 dict 都按插入顺序迭代，但这不等于按键排序；算法需要键有序时仍要显式排序

记一个实用直觉：想在循环里写 includes / indexOf / in 的时候，先停一下——那是 O(n) 的挨个翻，八成该换成哈希表。

### 一个最小例子

1. 两数之和：nums = [2, 7, 11, 15]，target = 9，找两个数加起来等于 9，返回它们的下标。

做法：从头到尾读数，每读一个数 x，先查登记簿「9 - x 之前见过吗」；没见过，就把 x 和它的下标记进簿子，再往前走。逐步推演（最右一列是每一步结束时簿子上的全部内容）：

| 步骤 | 读到的下标 i | 读到的数 x | 要找的另一半 9-x | 查簿结果 | 本步动作 | 登记簿内容（值→下标） |
| --- | --- | --- | --- | --- | --- | --- |
| 开场 | — | — | — | — | 簿子是空的，开始读数 | {}（空） |
| 1 | 0 | 2 | 7 | 簿上没有 7 | 把 2→0 记上簿 | {2→0} |
| 2 | 1 | 7 | 2 | 簿上有 2，记在下标 0 | 配对成功，返回 [0, 1] | {2→0} |

只读了两格、查了一次簿就得到答案（2 + 7 = 9，下标 0 和 1）。注意顺序必须是**先查后记**——查的时候，簿上只有前面读过的数，天然不会把「自己」错当成另一半。

对比一下暴力法：固定一个数，把它和后面每个数都加一遍看是不是 9。这个例子答案靠前，还看不出差距；把数组拉长到十万个数、答案在末尾，暴力要比较约 50 亿对，哈希表仍然一遍扫完。

多开一本登记簿的内存，换来了从 O(n^2) 到 O(n) 的提速——这就是刷题中最高频的**空间换时间**。

### 模板代码

「边查边记」模板（以两数之和为例）：

\`\`\`
// ===== JavaScript =====
function twoSum(nums, target) {
  const seen = new Map();                  // 登记簿：键 = 见过的数，值 = 它的下标
  for (let i = 0; i < nums.length; i++) {  // 从头到尾逐个读数
    const need = target - nums[i];         // 算出当前数还差多少，即「另一半」
    if (seen.has(need)) {                  // 先查：另一半之前见过吗？
      return [seen.get(need), i];          // 见过：取出它的下标，配对完成
    }
    seen.set(nums[i], i);                  // 没见过：把当前数和它的下标记上簿
  }
  return [];                               // 全部读完也没配上，返回空数组
}
\`\`\`

\`\`\`
# ===== Python =====
def two_sum(nums, target):
    seen = {}                              # 登记簿：键 = 见过的数，值 = 它的下标
    for i, x in enumerate(nums):           # 从头到尾逐个读数，i 是下标、x 是数值
        need = target - x                  # 算出当前数还差多少，即「另一半」
        if need in seen:                   # 先查：另一半之前见过吗？
            return [seen[need], i]         # 见过：取出它的下标，配对完成
        seen[x] = i                        # 没见过：把当前数和它的下标记上簿
    return []                              # 全部读完也没配上，返回空列表
\`\`\`

\`\`\`cpp
// ===== C++ =====
vector<int> twoSum(vector<int>& nums, int target) {
    unordered_map<int, int> seen;          // 登记簿：键 = 见过的数，值 = 它的下标
    for (int i = 0; i < (int)nums.size(); i++) {  // 从头到尾逐个读数
        int need = target - nums[i];       // 算出当前数还差多少，即「另一半」
        if (seen.count(need)) {            // 先查：另一半之前见过吗？
            return {seen[need], i};        // 见过：取出它的下标，配对完成
        }
        seen[nums[i]] = i;                 // 没见过：把当前数和它的下标记上簿
    }
    return {};                             // 全部读完也没配上，返回空数组
}
\`\`\`

如果只要判断「在不在」、不需要记下标，用更轻量的哈希集合（Set / set），只记键：

- 「在不在、重没重复」→ Set / set 就够
- 「在哪、第几次、配对的另一半」→ Map / dict，需要把值（下标、次数）一起记上簿

\`\`\`
// JavaScript：判断数组里有没有出现过数字 7
const seen = new Set();        // 只记「见过哪些数」的簿子
seen.add(2);                   // 记一笔：见过 2
seen.has(7);                   // 查一笔：见过 7 吗？返回 false
\`\`\`

\`\`\`
# Python：判断数组里有没有出现过数字 7
seen = set()                   # 只记「见过哪些数」的簿子
seen.add(2)                    # 记一笔：见过 2
7 in seen                      # 查一笔：见过 7 吗？返回 False
\`\`\`

\`\`\`cpp
// C++：判断数组里有没有出现过数字 7
unordered_set<int> seen;     // 只记「见过哪些数」的簿子
seen.insert(2);              // 记一笔：见过 2
seen.count(7);               // 查一笔：见过 7 吗？返回 0（表示没见过）
\`\`\`

还有一种廉价的番外：当键的范围很小且固定（比如只有 26 个小写字母）时，直接开一个定长**计数数组**当小哈希表用——下标就是键，格子里存出现次数，比真哈希表更省事：

\`\`\`
// JavaScript：统计字符串 s 里每个字母出现几次
const cnt = new Array(26).fill(0);   // 开 26 个格子，全部初始化为 0
for (const c of s) {                  // 逐个字符扫描
  cnt[c.charCodeAt(0) - 97]++;        // 编码减去 'a' 的编码 97，把字母映射到 0~25 号格子
}
\`\`\`

\`\`\`
# Python：统计字符串 s 里每个字母出现几次
cnt = [0] * 26                       # 开 26 个格子，全部初始化为 0
for c in s:                          # 逐个字符扫描
    cnt[ord(c) - ord('a')] += 1      # ord 取字符编码，减 'a' 把字母映射到 0~25 号格子
\`\`\`

\`\`\`cpp
// C++：统计字符串 s 里每个字母出现几次
int cnt[26] = {0};           // 开 26 个格子，全部初始化为 0
for (char c : s) {           // 逐个字符扫描
    cnt[c - 'a']++;          // 字符减 'a'，把字母映射到 0~25 号格子
}
\`\`\`

### 亲手跑一跑

把两数之和的推演过程打印出来，亲眼看着登记簿是怎么边查边记的：

\`\`\`run-js#two-sum-ledger
const nums = [2, 7, 11, 15];
const target = 9;
const seen = new Map();                        // 登记簿：数值 -> 下标
const show = () => (seen.size ? [...seen].map((p) => p[0] + '→' + p[1]).join(', ') : '(空)');
for (let i = 0; i < nums.length; i++) {
  const need = target - nums[i];               // 读到一个数，算出它的另一半
  console.log('第' + (i + 1) + '步: 读到 ' + nums[i] + ', 需要 ' + need + ', 登记簿: ' + show());
  if (seen.has(need)) {                        // 先查簿：另一半见过吗？
    console.log('  簿上有 ' + need + ' (在下标 ' + seen.get(need) + '), 配对成功! 答案 = [' + seen.get(need) + ', ' + i + ']');
    break;                                     // 找到答案，结束
  }
  seen.set(nums[i], i);                        // 没见过：记上簿，再往前走
}
\`\`\`

\`\`\`run-py#two-sum-ledger
nums = [2, 7, 11, 15]
target = 9
seen = {}                                      # 登记簿：数值 -> 下标
for i, x in enumerate(nums):
    need = target - x                          # 读到一个数，算出它的另一半
    table = ', '.join(str(k) + '→' + str(v) for k, v in seen.items()) or '(空)'
    print('第' + str(i + 1) + '步: 读到 ' + str(x) + ', 需要 ' + str(need) + ', 登记簿: ' + table)
    if need in seen:                           # 先查簿：另一半见过吗？
        print('  簿上有 ' + str(need) + ' (在下标 ' + str(seen[need]) + '), 配对成功! 答案 = [' + str(seen[need]) + ', ' + str(i) + ']')
        break                                  # 找到答案，结束
    seen[x] = i                                # 没见过：记上簿，再往前走
\`\`\`

C++：

\`\`\`run-cpp#two-sum-ledger
#include <iostream>
#include <string>
#include <unordered_map>
#include <vector>
using namespace std;

int main() {
    vector<int> nums = {2, 7, 11, 15};
    int target = 9;
    unordered_map<int, int> seen;          // 登记簿：数值 -> 下标
    for (int i = 0; i < (int)nums.size(); i++) {
        int x = nums[i];
        int need = target - x;             // 读到一个数，算出它的另一半
        string table = "";                 // 把登记簿当前内容拼成一行
        for (auto& p : seen) {
            if (!table.empty()) table += ", ";
            table += to_string(p.first) + "→" + to_string(p.second);
        }
        if (table.empty()) table = "(空)";
        cout << "第" << i + 1 << "步: 读到 " << x << ", 需要 " << need << ", 登记簿: " << table << endl;
        if (seen.count(need)) {            // 先查簿：另一半见过吗？
            cout << "  簿上有 " << need << " (在下标 " << seen[need] << "), 配对成功! 答案 = [" << seen[need] << ", " << i << "]" << endl;
            break;                         // 找到答案，结束
        }
        seen[x] = i;                       // 没见过：记上簿，再往前走
    }
    return 0;
}
\`\`\`

预期输出（三种语言逐字一致）：

\`\`\`
第1步: 读到 2, 需要 7, 登记簿: (空)
第2步: 读到 7, 需要 2, 登记簿: 2→0
  簿上有 2 (在下标 0), 配对成功! 答案 = [0, 1]
\`\`\`

第一步簿子是空的，查不到 7，就把 2 记进去；第二步一查就中。「先查后记」的顺序保证了 7 查到的是前面的 2，而不是它自己。

动手试试：把 nums 改成 [3, 2, 8, 4]、target 改成 6，先在纸上推一遍登记簿的变化，再跑代码对答案——能推对，这套机制就算长在你手里了。

### 直觉想法错在哪

最直觉的做法是：对每个数，都去数组里**挨个看**找它的另一半——两层 for 循环。写起来顺手，但每查一个数都要扫一遍数组（O(n)），n 个数就是 O(n^2)。n = 10^5 时约 50 亿次比较，必超时。

哈希表只是把这个动作换了个工具：**把「挨个翻」换成「登记簿」**。每见过一个数就记一笔，之后查「在不在、在哪」都是一步直达。多花一份内存，买回一个量级——这笔交易几乎总是划算的。

也有人会想到「先排序，再用二分找另一半」。想法不错，但这题有个硬约束：要返回的是**原数组的下标**，一排序，数和下标的对应关系就散了；把下标另存一份又绕回了「要记东西」——那不如一开始就记进哈希表。

### 常见错误

- **先记后查**：先把所有数塞进簿子再回头查，会把自己当成自己的另一半——比如 target = 6、数组里只有一个 3，会误以为 3 + 3 成立。必须走一步、查一步、记一步。
- **循环里用 includes / in 数组查存在**：这也是「挨个翻」，O(n)，藏在循环里又是 O(n^2)。Python 新手尤其爱写 x in list，把 list 换成 set 就回到 O(1)。
- **JS 拿普通对象 {} 当万能字典**：对象的键会被悄悄转成字符串，存数字键、数组键会出诡异问题。本题这种数字键，请用 Map。
- **Python 拿 list 当键**：list 不可哈希，会直接报错；要换成 tuple。
- **计数数组忘记减基准**：cnt[ord(c)] 直接当下标，26 个格子根本不够用，会越界。必须先减 ord('a')，映射到 0~25。

### 什么时候用

- 题目问「是否存在」「是否重复」「找配对 / 互补」→ 哈希表（Set / Map 二选一）
- 要**按位置**取、要保序处理、下标本身是信息 → 数组
- 键的范围小且固定（26 个字母、0~100 的分值）→ 计数数组
- 「数组不变、反复问某段区间和」→ 前缀和技巧（到 560. 和为 K 的子数组，会见到前缀和与哈希表联手）

同一套「登记簿」思想在题库里反复出现：

- 49. 字母异位词分组：把排序后的单词当 key，key 一样的单词归到一组
- 128. 最长连续序列：先把所有数丢进 Set，再 O(1) 判断「x - 1 存在吗」
- 41. 缺失的第一个正数：更进阶——把数组本身当登记簿用，下标当号码牌
- 238. 除自身以外数组的乘积：数组预处理思想（前后缀积），和前缀和一家亲

### 练习路径

- 1. 两数之和：把「边查边记」练到肌肉记忆
- 49. 字母异位词分组：体会「键怎么设计」——能把什么当 key，决定了哈希表能干什么活
- 128. 最长连续序列：体会「全丢进 Set 再 O(1) 查存在」的威力
- 560. 和为 K 的子数组：前缀和 + 哈希表联手，发现和两数之和是同一个套路
- 438. 找到字符串中所有字母异位词：计数数组实战
`,
};
