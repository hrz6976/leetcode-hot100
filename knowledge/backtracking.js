// 知识文章：回溯算法
// 数据格式约定（所有知识文章遵循）：
// - slug: 英文短横线命名，同时是文件名与路由
// - tags: 与题库中题目的 tags 匹配，用于自动关联题目
// - relatedProblems（可选）: 显式指定关联题目 id，优先于 tags 匹配
// - content: Markdown 子集（### 标题、- 列表、``` 代码块、`行内代码`）
export default {
  slug: 'backtracking',
  title: '回溯算法',
  intro: '在解空间树上做选择、递归、撤销选择，穷举所有合法方案',
  tags: ['回溯', '数组', '字符串', '矩阵'],
  relatedProblems: [17, 22, 39, 46, 51, 78, 79, 131],

  content: `
> 提示：读这篇文章前，最好已经具备这些基础：
> 1. 会写 for 循环，会定义、调用函数；
> 2. 知道什么是「递归」——函数自己调用自己（《二叉树与递归》讲过）；
> 3. 大致知道 DFS 是「一条路走到底，走不通再回头」（《图的 DFS 与 BFS》讲过）。

去超市试吃：拿起牙签尝一口，不好吃就放下样品、空着手离开摊位，再去下一家。关键动作是「放下」——放下之后，你的手和刚进门时一样空，下一家尝到的才是原味，不串味。

回溯算法干的就是这件事：**做一个选择** → 顺着它往下走 → 走到底或走不通后**撤销这个选择**、恢复原样 → 换一种选法再试。它的目标是把所有可能的方案一个不落地找出来。

### 是什么

回溯 = 有章法的穷举。

面对「列出所有可能方案」的题目，可以把它想成走一棵「选择树」：每往下走一层，就多做了一个决定；走到叶子，就攒出了一个完整方案。回溯就是沿着这棵树一条路走到底（这就是 DFS），触底后退回上一个岔路口，换条路继续——直到每条路都走过。

几个术语对照（后面会反复用到）：

- **解空间树**：脑海里那棵「选择树」，每个节点代表当前已做的部分选择
- **路径 path**：从树根到当前节点手里已经做的选择，用一个数组记录
- **撤销选择**：退回上一层时，把上一步放进路径的东西拿出来，恢复原状
- **剪枝**：某个分支明显不可能长出答案时，提前不走，省掉整棵子树

「撤销」是回溯的灵魂。不撤销，上一条分支的选择会留在路径里，后面所有分支的答案都会被污染——就像试吃时不放下样品，第二家的味道全串了。

回溯的复杂度通常是指数级（n 个元素有 2ⁿ 个子集），所以这类题的数据范围都很小（n 一般不超过 20）。**看到「小数据范围 + 要求列出所有方案」，就该想到回溯。**

### 一个最小例子

用 3 个数字 \`[1, 2, 3]\`，生成它的**所有子集**（子集 = 从里面随便挑几个数，也可以一个都不挑）。

对每个数字只有两个决定：**选，或不选**。3 个数字就是 3 层决定，每层分出两条岔路，一共有 2 × 2 × 2 = 8 条走法，正好对应 8 个子集。

下面按 DFS 的顺序逐步推演：每一层先走「不选」的岔路，触底退回来后改成「选」。请盯住 path（手里的篮子）——看清它什么时候放东西、什么时候拿出来。

| 步骤 | 正在为哪个数字做决定 | 本步动作 | path（当前篮子） | 是否得到一个子集 |
| --- | --- | --- | --- | --- |
| 1 | 数字 1 | 不选 1，走向下一层 | [] | 否 |
| 2 | 数字 2 | 不选 2，走向下一层 | [] | 否 |
| 3 | 数字 3 | 不选 3，决定做完了 | [] | 是，记录空集 [] |
| 4 | 数字 3 | 退回，改成选 3 | [3] | 是，记录 [3] |
| 5 | 数字 2 | 退回到第 2 层（3 先从篮子拿走），改选 2 | [2] | 否 |
| 6 | 数字 3 | 不选 3，决定做完了 | [2] | 是，记录 [2] |
| 7 | 数字 3 | 退回，改成选 3 | [2, 3] | 是，记录 [2, 3] |
| 8 | 数字 1 | 一路退回第 1 层（篮子已被掏空），改选 1 | [1] | 否 |
| 9 | 数字 2 | 不选 2，走向下一层 | [1] | 否 |
| 10 | 数字 3 | 不选 3，决定做完了 | [1] | 是，记录 [1] |
| 11 | 数字 3 | 退回，改成选 3 | [1, 3] | 是，记录 [1, 3] |
| 12 | 数字 2 | 退回到第 2 层（3 先从篮子拿走），改选 2 | [1, 2] | 否 |
| 13 | 数字 3 | 不选 3，决定做完了 | [1, 2] | 是，记录 [1, 2] |
| 14 | 数字 3 | 退回，改成选 3 | [1, 2, 3] | 是，记录第 8 个 [1, 2, 3] |

8 个子集：[]、[3]、[2]、[2, 3]、[1]、[1, 3]、[1, 2]、[1, 2, 3]，一个不多一个不少。

整张表只做两件事：**每层开两个岔（选 / 不选）；每次退回上层之前，先把这个岔放进篮子的东西拿走**——这就是下一节代码的全部逻辑。

### 模板代码

JavaScript 版本（就是上面那个例子的完整代码）：

\`\`\`
// JavaScript：生成所有子集
const nums = [1, 2, 3];
const result = [];              // 大袋子：收集所有答案
const path = [];                // 篮子：当前已经选了哪些数
function backtrack(i) {         // i = 本轮要为 nums[i] 做决定
  if (i === nums.length) {      // 所有数字都决定过了 → 到达叶子
    result.push([...path]);     // 存一份篮子的拷贝（关键！不能存篮子本身）
    return;                     // 本分支走完，退回上一层
  }
  backtrack(i + 1);             // 岔路一：不选 nums[i]，直接去下一层
  path.push(nums[i]);           // 岔路二：选它 → 1. 做选择
  backtrack(i + 1);             //           → 2. 带着新篮子往下递归
  path.pop();                   //           → 3. 撤销选择，篮子恢复原样
}
backtrack(0);                   // 从「决定第 0 个数字」开始
\`\`\`

Python 版本：

\`\`\`
# Python：生成所有子集
nums = [1, 2, 3]
result = []                     # 大袋子：收集所有答案
path = []                       # 篮子：当前已经选了哪些数
def backtrack(i):               # i = 本轮要为 nums[i] 做决定
    if i == len(nums):          # 所有数字都决定过了 → 到达叶子
        result.append(path.copy())  # 存一份篮子的拷贝（关键！不能存篮子本身）
        return                  # 本分支走完，退回上一层
    backtrack(i + 1)            # 岔路一：不选 nums[i]，直接去下一层
    path.append(nums[i])        # 岔路二：选它 → 1. 做选择
    backtrack(i + 1)            #           → 2. 带着新篮子往下递归
    path.pop()                  #           → 3. 撤销选择，篮子恢复原样
backtrack(0)                    # 从「决定第 0 个数字」开始
\`\`\`

C++ 版本：

\`\`\`cpp
// C++：生成所有子集
vector<int> nums = {1, 2, 3};
vector<vector<int>> result; // 大袋子：收集所有答案
vector<int> path;           // 篮子：当前已经选了哪些数
function<void(int)> backtrack = [&](int i) {  // i = 本轮要为 nums[i] 做决定
    if (i == (int)nums.size()) {  // 所有数字都决定过了 → 到达叶子
        result.push_back(path);   // 存一份篮子的拷贝（vector 放进去的就是拷贝）
        return;                   // 本分支走完，退回上一层
    }
    backtrack(i + 1);             // 岔路一：不选 nums[i]，直接去下一层
    path.push_back(nums[i]);      // 岔路二：选它 → 1. 做选择
    backtrack(i + 1);             //           → 2. 带着新篮子往下递归
    path.pop_back();              //           → 3. 撤销选择，篮子恢复原样
};
backtrack(0);                     // 从「决定第 0 个数字」开始
\`\`\`

所有回溯题共用同一个骨架：**终止条件 → 逐个候选做选择 → 递归 → 撤销**。换题目只是换两件事——每层能选什么、什么时候算到头：

- **排列**（46 全排列）：每层可以在所有数里挑，但挑过的不能再挑，需要一个 used 数组做标记，标记也要跟随撤销
- **组合**（39 组合总和）：每层只能从 startIndex 往后挑，保证下标递增，结果天然不重复；数字可重复使用时，递归传 backtrack(i) 而不是 backtrack(i + 1)
- **剪枝类**（22 括号生成）：做选择前先检查合法性，不合法的岔路干脆不走

### 亲手跑一跑

下面三段是同一份代码的 JS、Python 和 C++ 版：生成 \`[1, 2, 3]\` 的所有子集，并把每一层的决定打印出来。建议边看输出边对照上一节的表格。

JavaScript：

\`\`\`run-js#subsets-walkthrough
const nums = [1, 2, 3];                    // 要生成子集的数组
const result = [];                         // 大袋子
const path = [];                           // 篮子
const show = (arr) => '[' + arr.join(', ') + ']';
function backtrack(i) {                    // 正在为 nums[i] 做决定
  if (i === nums.length) {                 // 决定做完了
    console.log('  得到一个子集：' + show(path));
    result.push([...path]);
    return;
  }
  console.log('第 ' + i + ' 层：不选 ' + nums[i] + '，篮子 = ' + show(path));
  backtrack(i + 1);
  path.push(nums[i]);
  console.log('第 ' + i + ' 层：改选 ' + nums[i] + '，篮子 = ' + show(path));
  backtrack(i + 1);
  path.pop();                              // 撤销选择，篮子恢复成进入本层前的样子
}
backtrack(0);
console.log('一共生成 ' + result.length + ' 个子集');
\`\`\`

Python：

\`\`\`run-py#subsets-walkthrough
nums = [1, 2, 3]                        # 要生成子集的数组
result = []                             # 大袋子
path = []                               # 篮子
def show(arr):
    return '[' + ', '.join(map(str, arr)) + ']'
def backtrack(i):                       # 正在为 nums[i] 做决定
    if i == len(nums):                  # 决定做完了
        print('  得到一个子集：' + show(path))
        result.append(path.copy())
        return
    print('第 ' + str(i) + ' 层：不选 ' + str(nums[i]) + '，篮子 = ' + show(path))
    backtrack(i + 1)
    path.append(nums[i])
    print('第 ' + str(i) + ' 层：改选 ' + str(nums[i]) + '，篮子 = ' + show(path))
    backtrack(i + 1)
    path.pop()                          # 撤销选择，篮子恢复成进入本层前的样子
backtrack(0)
print('一共生成 ' + str(len(result)) + ' 个子集')
\`\`\`

C++：

\`\`\`run-cpp#subsets-walkthrough
#include <iostream>
#include <string>
#include <vector>
using namespace std;

vector<int> nums = {1, 2, 3};              // 要生成子集的数组
vector<vector<int>> result;                // 大袋子
vector<int> path;                          // 篮子

string show(const vector<int>& arr) {
    string s = "[";
    for (int i = 0; i < (int)arr.size(); i++) {
        if (i > 0) s += ", ";
        s += to_string(arr[i]);
    }
    return s + "]";
}

void backtrack(int i) {                    // 正在为 nums[i] 做决定
    if (i == (int)nums.size()) {           // 决定做完了
        cout << "  得到一个子集：" << show(path) << endl;
        result.push_back(path);
        return;
    }
    cout << "第 " << i << " 层：不选 " << nums[i] << "，篮子 = " << show(path) << endl;
    backtrack(i + 1);
    path.push_back(nums[i]);
    cout << "第 " << i << " 层：改选 " << nums[i] << "，篮子 = " << show(path) << endl;
    backtrack(i + 1);
    path.pop_back();                       // 撤销选择，篮子恢复成进入本层前的样子
}

int main() {
    backtrack(0);
    cout << "一共生成 " << result.size() << " 个子集" << endl;
    return 0;
}
\`\`\`

三个版本的输出完全一致，都是下面这样：

\`\`\`
第 0 层：不选 1，篮子 = []
第 1 层：不选 2，篮子 = []
第 2 层：不选 3，篮子 = []
  得到一个子集：[]
第 2 层：改选 3，篮子 = [3]
  得到一个子集：[3]
第 1 层：改选 2，篮子 = [2]
第 2 层：不选 3，篮子 = [2]
  得到一个子集：[2]
第 2 层：改选 3，篮子 = [2, 3]
  得到一个子集：[2, 3]
第 0 层：改选 1，篮子 = [1]
第 1 层：不选 2，篮子 = [1]
第 2 层：不选 3，篮子 = [1]
  得到一个子集：[1]
第 2 层：改选 3，篮子 = [1, 3]
  得到一个子集：[1, 3]
第 1 层：改选 2，篮子 = [1, 2]
第 2 层：不选 3，篮子 = [1, 2]
  得到一个子集：[1, 2]
第 2 层：改选 3，篮子 = [1, 2, 3]
  得到一个子集：[1, 2, 3]
一共生成 8 个子集
\`\`\`

留意输出顺序：先一路「不选」走到底拿到空集，然后一层层退回、逐层「改选」——和上一节表格里的 14 步一一对应。

### 直觉想法错在哪

最容易先想到的是：「那就用 for 循环枚举呗」。结果一动手就卡住：要枚举「随便挑几个数」，循环的层数得等于数组长度 n——n = 3 时能硬着头皮写 3 层嵌套，n = 10 呢？层数没法手写。**递归一层套一层，正好相当于「层数可变的循环」**，这就是为什么穷举离不开递归。

第二个直觉：「试错了就重新拿个空篮子再试嘛，干嘛非要撤销」。每层新开一个篮子确实也能写对（比如每层把数组复制一份传下去），但每层都复制一次数组，时间和内存白白翻倍——回溯的精髓正是**全场只用一只篮子，进出都收拾干净**，把空间省到极致。

### 常见错误

- **存答案时直接 result.push(path)**：存进去的是篮子本身（同一个数组），后面篮子一改，之前存的「答案」全跟着变。必须存拷贝：JS 用 \`[...path]\`，Python 用 \`path.copy()\`。容易犯是因为直觉里「放进去」就是「存好了」，不知道数组是按引用共享的。
- **push 了忘记 pop 撤销**：下一个分支的篮子里还留着上一个分支的东西，答案互相污染。容易犯是因为满脑子想「怎么往下走」，忘了本层循环里还有下一个候选要用同一只篮子。
- **漏写或写错终止条件**：递归没有出口会无限递归，直到栈溢出（JS 报 Maximum call stack size exceeded）。容易犯是因为只设计了「怎么走」，没设计「什么时候停」。
- **排列题照抄子集代码、不用 used**：子集每层只决定一个数，所以不需要标记；排列每层要在所有数里挑，不用 used 就会选出 [1, 1, 2] 这种假排列。
- **剪枝条件写反**：比如 22 题放右括号的条件是「已放的右括号少于左括号」，写反就会生成一堆非法串。条件写在岔路口上，一念之差就反——写完务必用 n = 1、2 手算验证。

### 什么时候用

- **要求列出所有**子集 / 排列 / 组合 / 合法序列：78 子集、46 全排列、39 组合总和、22 括号生成、17 电话号码的字母组合
- **在棋盘 / 网格上试探着填**：51 N 皇后、79 单词搜索（在格子上做标记，试完必须恢复）
- **数据范围很小**（n ≤ 20 左右）：指数级也跑得动，这是出题人在暗示「可以暴力枚举」
- 反过来，只问「有多少种」而不用列出方案时，通常有更快的动态规划，见《动态规划入门》

### 练习路径

按这个顺序刷，每题的预期收获写在后面：

- **78 子集**：把本文骨架原样跑通，吃透「每层选 / 不选」
- **46 全排列**：学会 used 数组，记住「标记和篮子一样，要做选择 + 撤销」
- **39 组合总和**：数字可重复选时递归 backtrack(i)，体会「允许重复选」和「解不重复」如何同时成立
- **22 括号生成**：第一次写剪枝——做选择之前先判断合法性
- **79 单词搜索**：二维网格上的回溯，学会「原地标记 + 用完恢复」
- **51 N 皇后**：综合演练，把列和两条对角线的合法性检查全部写对
`,
};
