// 知识文章：二叉树与递归
// 数据格式约定（所有知识文章遵循）：
// - slug: 英文短横线命名，同时是文件名与路由
// - tags: 与题库中题目的 tags 匹配，用于自动关联题目
// - content: Markdown 子集（### 标题、- 列表、``` 代码块、`行内代码`）
export default {
  slug: 'binary-tree',
  title: '二叉树与递归',
  intro: '递归两大范式、DFS/BFS 遍历模板与二叉搜索树的有序性',
  tags: ['树', '二叉树', '深度优先搜索', '广度优先搜索', '递归'],
  relatedProblems: [94, 98, 101, 102, 104, 199, 226, 230, 236, 543],

  content: `
> 提示：这篇从零讲递归，没有树的基础可以直接读。你只需要会两件事：知道数组怎么存东西（见《数组与哈希表》）、知道函数可以调用自己以外的函数。文中说「系统用栈帮你记事」，栈是什么在《栈、队列与单调栈》里有，但不知道也不影响理解本文。

想象一张公司组织架构图：CEO 在最顶上，底下每个经理最多带两个直接下属——左下属和右下属，有些员工是光杆司令（没有下属）。这就是二叉树。CEO 想知道全公司多少人？他不用自己数，只问两个下属各报人数，收齐两个数字、加上自己，报上去就行。这个「**把任务丢给下属、收齐结果再汇总**」的动作，就是递归，也是二叉树所有题目的万能钥匙。

### 是什么

先用大白话：二叉树就是一张「每个节点最多两个孩子的层级图」。最顶上那位叫**根**，每个节点手里攥着最多两枚指针，分别指向**左孩子**和**右孩子**；没有孩子的节点叫**叶子**；指针指向「空」（null / None）表示这个位置没人。

术语对照：

| 大白话 | 术语 | 在组织架构图里 |
| --- | --- | --- |
| 一个数据点 | 节点 node | 一个员工 |
| 最顶上没人管的 | 根 root | CEO |
| 两枚指针指向下级 | 左/右孩子 left/right | 左右两个直接下属 |
| 没有下属的 | 叶子 leaf | 一线员工 |
| 整棵左下属团队 | 左子树 | 左下属及其全部下级的下级 |
| 走一遍所有人 | 遍历 | 巡视组按某种顺序点名 |

还有两个名词提前见个面，题解里满坑满谷：**深度**是从根往下数到该节点经过的边数（根的深度为 0），**高度**是从该节点往下数到最远叶子经过的边数（叶子的高度为 0）。一个从上往下量、一个从下往上量，别混。

为什么树题几乎全是递归？因为树本身就是递归定义的：**一棵树 = 根节点 + 左子树 + 右子树**，而「左子树」本身又是一棵完整的树，结构和整体一模一样。既然如此——处理好一个节点 + 把左右子树**原样委托**给同一个函数，问题就解决了。写递归时只想两件事：这个函数**返回什么**？拿到左右子树的结果后**怎么合并**？

### 一个最小例子

用最小的树练手：三个节点，根是 1，左孩子 2，右孩子 3（2 和 3 都是叶子）：

\`\`\`
    1
   / \\
  2   3
\`\`\`

「遍历」就是把所有节点按某种顺序各访问一次。三种经典顺序的区别**只在于什么时候写下「自己」**：

| 遍历 | 口诀 | 写下自己的时机 | 本例访问顺序 |
| --- | --- | --- | --- |
| 前序 | 根 左 右 | 一到任先写自己 | 1 → 2 → 3 |
| 中序 | 左 根 右 | 左子树交差后写自己 | 2 → 1 → 3 |
| 后序 | 左 右 根 | 两边都交差才写自己 | 2 → 3 → 1 |

以中序（左、根、右）为例，把「任务丢给下属」的完整过程走一遍。每个人都是同一套规矩：先派左下属，左下属交差就写下自己，再派右下属。

| 步 | 当前处理 | 本步发生了什么 | 写下的结果 |
| --- | --- | --- | --- |
| 1 | 节点 1 | 按规矩先派左下属 2，自己原地等 | （空） |
| 2 | 节点 2 | 没左下属可派；写下 2；也没右下属，2 交差 | [2] |
| 3 | 回到节点 1 | 左子树交差了，写下 1；再派右下属 3 | [2, 1] |
| 4 | 节点 3 | 没左下属；写下 3；没右下属，3 交差 | [2, 1, 3] |
| 5 | 回到节点 1 | 右子树也交差了，1 收工，整棵树遍历完 | [2, 1, 3] |

亲眼看到了吗：**没有一个节点需要「管全局」**——每个节点只做三件事（派左、写自己、派右），层层委托之后整棵树自然被走了一遍。这正是递归的思考方式：你写代码时只描述「一个节点该干什么」，别去操心整棵树长什么样。

### 模板代码

模板一：中序遍历。注意整个函数就是上表那三句话。

JavaScript 版本（每行都有注释）：

\`\`\`
// JavaScript
function inorderTraversal(root) {
  const ans = [];                    // 收集写下的节点值
  const dfs = (node) => {            // dfs：处理「以 node 为根的子树」
    if (node === null) return;       // 空位置：没人，直接交差
    dfs(node.left);                  // 1) 先把任务派给左下属
    ans.push(node.val);              // 2) 左子树交差，写下自己（中序）
    dfs(node.right);                 // 3) 再把任务派给右下属
  };
  dfs(root);                         // 从最顶上的根开始派活
  return ans;                        // 收齐的名单就是答案
}
\`\`\`

Python 版本（逻辑一模一样）：

\`\`\`
# Python
def inorder_traversal(root):
    ans = []                  # 收集写下的节点值
    def dfs(node):            # dfs：处理「以 node 为根的子树」
        if node is None:      # 空位置：没人，直接交差
            return
        dfs(node.left)        # 1) 先把任务派给左下属
        ans.append(node.val)  # 2) 左子树交差，写下自己（中序）
        dfs(node.right)       # 3) 再把任务派给右下属
    dfs(root)                 # 从最顶上的根开始派活
    return ans                # 收齐的名单就是答案
\`\`\`

C++ 版本（逻辑一模一样）：

\`\`\`cpp
// C++
vector<int> ans;                  // 收集写下的节点值
void dfs(TreeNode* node) {        // dfs：处理「以 node 为根的子树」
    if (node == nullptr) return;  // 空位置：没人，直接交差
    dfs(node->left);              // 1) 先把任务派给左下属
    ans.push_back(node->val);     // 2) 左子树交差，写下自己（中序）
    dfs(node->right);             // 3) 再把任务派给右下属
}
vector<int> inorderTraversal(TreeNode* root) {
    dfs(root);                    // 从最顶上的根开始派活
    return ans;                   // 收齐的名单就是答案
}
\`\`\`

前序、后序不用背新代码——**只需把 ans 那一行挪位置**：放两声 dfs 之前是前序，放之后是后序。三种遍历是同一份代码的三种摆放。

模板二：求树的最大深度（104）。这是「收齐结果再汇总」的纯享版——函数**返回**每棵子树的深度，父节点拿两个返回值算出自己这棵：

\`\`\`
// JavaScript
function maxDepth(root) {
  if (root === null) return 0;             // 空位置：高度 0，报上去
  const left = maxDepth(root.left);        // 问左下属：你那边最深几层
  const right = maxDepth(root.right);      // 问右下属：你那边最深几层
  return Math.max(left, right) + 1;        // 收齐两边，取大者 + 自己这层
}
\`\`\`

\`\`\`
# Python
def max_depth(root):
    if root is None:              # 空位置：高度 0，报上去
        return 0
    left = max_depth(root.left)   # 问左下属：你那边最深几层
    right = max_depth(root.right) # 问右下属：你那边最深几层
    return max(left, right) + 1   # 收齐两边，取大者 + 自己这层
\`\`\`

\`\`\`cpp
// C++
int maxDepth(TreeNode* root) {
    if (root == nullptr) return 0;     // 空位置：高度 0，报上去
    int left = maxDepth(root->left);   // 问左下属：你那边最深几层
    int right = maxDepth(root->right); // 问右下属：你那边最深几层
    return max(left, right) + 1;       // 收齐两边，取大者 + 自己这层
}
\`\`\`

这两种写法对应递归的两大范式：把信息**装在返回值里带回来**（自底向上，如 maxDepth），或者**顺着参数往下传**（自顶向下，比如把当前深度传给孩子）。有时候「函数要返回的信息」和「题目要的答案」不是一回事（如 543 直径），就用一个外面的大变量顺手收集答案，函数照常返回它该返回的。

### 亲手跑一跑

建树、跑中序遍历，每写下一个节点就打印当时的名单。跟着输出对照上面的第 2、3、4 步。

\`\`\`run-js#inorder-walkthrough
const root = { val: 1,
  left: { val: 2, left: null, right: null },
  right: { val: 3, left: null, right: null } };
const ans = [];
function dfs(node) {
  if (node === null) return;
  dfs(node.left);
  ans.push(node.val);
  console.log('节点 ' + node.val + ' 写下自己  结果: ' + ans.join(' -> '));
  dfs(node.right);
}
dfs(root);
console.log('中序遍历最终结果: ' + ans.join(' -> '));
\`\`\`

实际运行输出：

\`\`\`
节点 2 写下自己  结果: 2
节点 1 写下自己  结果: 2 -> 1
节点 3 写下自己  结果: 2 -> 1 -> 3
中序遍历最终结果: 2 -> 1 -> 3
\`\`\`

\`\`\`run-py#inorder-walkthrough
root = {'val': 1,
        'left': {'val': 2, 'left': None, 'right': None},
        'right': {'val': 3, 'left': None, 'right': None}}
ans = []
def dfs(node):
    if node is None:
        return
    dfs(node['left'])
    ans.append(node['val'])
    print(f"节点 {node['val']} 写下自己  结果: {' -> '.join(map(str, ans))}")
    dfs(node['right'])
dfs(root)
print('中序遍历最终结果: ' + ' -> '.join(map(str, ans)))
\`\`\`

C++：

\`\`\`run-cpp#inorder-walkthrough
#include <iostream>
#include <string>
#include <vector>
using namespace std;

struct TreeNode {
    int val;
    TreeNode* left;
    TreeNode* right;
    TreeNode(int v) : val(v), left(nullptr), right(nullptr) {}
};

vector<int> ans;                           // 收集写下的节点值

string show(const vector<int>& arr) {      // 拼成 "2 -> 1 -> 3" 的样子
    string s = "";
    for (int i = 0; i < (int)arr.size(); i++) {
        if (i > 0) s += " -> ";
        s += to_string(arr[i]);
    }
    return s;
}

void dfs(TreeNode* node) {
    if (node == nullptr) return;
    dfs(node->left);
    ans.push_back(node->val);
    cout << "节点 " << node->val << " 写下自己  结果: " << show(ans) << endl;
    dfs(node->right);
}

int main() {
    TreeNode* root = new TreeNode(1);
    root->left = new TreeNode(2);
    root->right = new TreeNode(3);
    dfs(root);
    cout << "中序遍历最终结果: " << show(ans) << endl;
    return 0;
}
\`\`\`

实际运行输出：

\`\`\`
节点 2 写下自己  结果: 2
节点 1 写下自己  结果: 2 -> 1
节点 3 写下自己  结果: 2 -> 1 -> 3
中序遍历最终结果: 2 -> 1 -> 3
\`\`\`

> 例子：把 ans.append / ans.push 那一行挪到两声 dfs 之前或之后各跑一次，输出会立刻变成前序（1 -> 2 -> 3）和后序（2 -> 3 -> 1）——亲手验证「三种遍历只差一行代码的位置」。

### 直觉想法错在哪

零基础面对「遍历这棵树」的第一反应是写循环：从 1 往下走，走到 2……然后呢？还得**回来**走 3——可循环没有一个地方记住「回哪儿、回来之后接着干什么」。树和数组的本质差别就是它有分叉：每往下走一步，都欠下一笔「回头债」。靠变量硬记这笔账，越记越乱。

递归恰好自带账本：每调用一层 dfs，系统就自动把这个函数「暂停在哪一行、局部变量是什么」压进调用栈，子调用返回时自动恢复现场。所谓「系统帮你维护栈」就是这个意思——你欠的每一笔回头债，系统都替你用栈记着呢。理解了这一点，再看到「用栈把递归改写成迭代」的题解就不慌了：那只是把系统暗地里做的事，搬到明面上自己做。

### 常见错误

- **漏写空节点终止条件**：不写 if (!node) return，递归走到叶子脚下还往里钻，访问 null.val 直接崩。容易犯是因为总想着「题目给了树」，忘了左右孩子经常是不存在的空位置
- **前中后序背混**：把「写下自己」的那一行放错位置。容易犯是因为死记「前中后」三个名字——记住它说的是**根（自己）**在左、右之间的出场顺序就再也不会混
- **在一层里操整棵树的心**：写递归时忍不住想「根节点要不要判断一下整棵树平不平衡」，越想越复杂。正确姿势是**假设左右下属已经给了正确答案**，你只写「当前这一层怎么合并」
- **函数返回值含义前后不一**：一会儿返回深度一会儿返回布尔值，写到一半自己先晕。动手前先写一行注释：「dfs(node) 返回 xx」，全队（每层递归）统一口径
- **Python 里改外层变量忘写 nonlocal**：内层函数 ans = ... 会变成新建局部变量，外层答案永远是初始值（模板里用 append 改内容不用声明；一旦要重新赋值就要 nonlocal）

### 什么时候用

- 题目给的数据结构是树（ TreeNode 、每个节点带 left / right ）：默认先想递归，树几乎等于递归
- 答案能从左右子树的答案**合并**出来（深度、节点数、是否平衡、是否对称）：自底向上，用返回值带回来，如 104、226、101
- 需要**带着状态往下走**（当前深度、路径上的和）：自顶向下，靠参数往下传
- 「函数要返回的」和「题目要求的」是两件事：返回值照旧，另开变量收集答案，如 543 直径
- 二叉搜索树（BST，左 < 根 < 右对每棵子树都成立）：**中序遍历它得到的就是从小到大排好的序列**，98 验证、230 第 k 小都是中序的变形
- 要**按层**处理（102 层序遍历、199 右视图）：换 BFS，用队列一层层出队（队列用法见《栈、队列与单调栈》）

### 练习路径

104 最大深度（背下「收齐结果 + 1」，感受四行递归的威力）→ 226 翻转二叉树（换个姿势写递归，左右互换别想复杂）→ 94 中序遍历（把模板一默写到肌肉记忆，前/后序各挪一行验证）→ 102 层序遍历（认识树的 BFS，为《图的 DFS 与 BFS》热身）→ 543 直径（体会「返回值」与「答案」分离，最长路径可以不过根）→ 98 验证 BST（只比父子是错的，要给每个节点带上下界）→ 230 第 k 小（中序数第 k 个，吃透 BST 有序性）→ 236 最近公共祖先（自底向上返回值的进阶运用，想通返回值里装的是什么）。
`,
};
