// 知识文章：位运算、投票与原地处理
export default {
  "slug": "bit-tricks",
  "title": "位运算、投票与原地处理",
  "intro": "理解技巧依赖的题目条件，同时留意整数范围与重复元素。",
  "tags": ["位运算","数组","计数"],
  "relatedProblems": [
    {
      "id": 136,
      "stage": "core",
      "reason": "用异或消去成对出现的值"
    },
    {
      "id": 169,
      "stage": "core",
      "reason": "证明多数元素抵消后仍会留下"
    },
    {
      "id": 75,
      "stage": "practice",
      "reason": "区分已分类部分与未知部分"
    },
    {
      "id": 41,
      "stage": "practice",
      "reason": "把合法值放到对应位置"
    }
  ],
  content: `
位运算直接处理整数的二进制位。它在少数题里能明显简化代码，但前提是你知道题目保证了什么，以及所用语言怎样表示整数。

这一篇还介绍原地交换和多数投票。它们不是同一个算法，只是都常用于减少额外存储。

### 异或：相同的位得到 0，不同的位得到 1

整数 a ^ b 表示按位异或。常用性质是 a^a=0、a^0=a，并且交换或改变括号顺序不影响结果。

第 136 题保证：除了一个元素出现一次，其余每个元素都恰好出现两次。把所有数异或起来，出现两次的值相互抵消，只剩出现一次的值。

例如 [4,1,2,1,2]，重排计算顺序后相当于 4^(1^1)^(2^2)=4。

这是题目条件与运算性质配合得到的结论。若其他数出现三次，或者有两个数各出现一次，就不能原样使用这段代码。

\`\`\`run-py#bit-tricks-v2-demo
def single_number(nums):
    answer = 0
    for x in nums:
        answer ^= x
    return answer

print(single_number([4, 1, 2, 1, 2]))
\`\`\`

\`\`\`run-js#bit-tricks-v2-demo
function singleNumber(nums) {
  let answer = 0;
  for (const x of nums) answer ^= x;
  return answer;
}
// 此示例适用于题目给定的 32 位整数范围。
console.log(singleNumber([4, 1, 2, 1, 2]));
\`\`\`

\`\`\`run-cpp#bit-tricks-v2-demo
#include <iostream>
#include <vector>
using namespace std;
int singleNumber(const vector<int>& nums) {
    int answer = 0;
    for (int x : nums) answer ^= x;
    return answer;
}
int main() { cout << singleNumber({4, 1, 2, 1, 2}) << '\\n'; }
\`\`\`

时间 O(n)。对于常见固定宽度整数，额外空间 O(1)；若讨论任意大整数，单次位操作和存储大小也会依赖位数。

### 清掉最低的一个 1

对正整数 x，x&(x−1) 会清掉二进制最低的那个 1。例如 12 的二进制是 1100，11 是 1011，按位与得到 1000，也就是 8。

原因是减一会把最低的 1 变成 0，并把它后面的 0 变成 1；按位与后，这些位置都变成 0，更高位保持不变。

因此对正整数，如果 x&(x−1)==0，就只有一个 1，它是 2 的幂。x>0 不能省略，否则 0 也会误通过。

x&(-x) 常用来保留最低的那个 1，通常叫 lowbit。它依赖整数的表示语义，在树状数组里常见；当前 Hot 100 的基础练习不需要为了这个技巧提前学习整套数据结构。

### 三种语言的整数差异

JavaScript 对 Number 使用 ^、& 等位运算时，会转换为 32 位整数。超出范围的值会被截断，不能把只适用于 32 位的示例当成任意整数算法。需要更大的精确整数时，可以考虑 BigInt，但它不能和 Number 混合运算。

Python 整数可以增长到任意位数，但负数做位运算时要按它的无限符号扩展语义理解。对负数反复使用 x&=x−1，不会像正整数那样很快到 0。

C++ 要注意有符号整数溢出以及移位范围。讨论位掩码时常用合适宽度的无符号类型。对 int 的最小值取负可能溢出，不能无条件照抄 x&(-x)。

### 多数投票：不同的元素成对抵消

第 169 题保证存在一个出现次数超过 n/2 的元素。维护候选 candidate 和计数 count。count 为 0 时换候选；读到相同值就加一，不同值就减一。

可以把每次减一理解为删去一对不同元素。真正的多数元素比所有其他元素加起来还多，成对删除后，它仍然会成为最后候选。

count 不是候选在原数组中的真实出现次数，而是抵消后留下的数量。若题目没有保证多数元素存在，最后必须再统计候选出现次数，验证是否真的超过一半。

### 原地放回应该在的位置

第 41 题找缺失的第一个正数。长度为 n 的数组，答案一定在 1 到 n+1 之间。尝试把合法值 x 放到下标 x−1，让值和位置对应。

处理 nums[i] 时，只要它在 [1,n] 内，并且目标位置没有同样的值，就交换过去，继续检查新的 nums[i]。目标位置已有相同值时必须停止，否则重复元素可能让交换死循环。

放好后，从头找第一个 nums[i]≠i+1 的位置；没有就返回 n+1。每次有效交换至少把一个合法值放到最终位置，所以总交换次数是 O(n)，不是看到 while 就一定 O(n²)。算法会改变输入，额外空间 O(1)。

### 技巧应该怎样用

先做第 136 题，准确说出出现次数条件；再做第 169 题，理解抵消而非真实计数；然后做第 75 题“颜色分类”和第 41 题。

第 75 题交换右端元素到当前位置后，当前位置的新值还没检查，不能立刻向前走。这和原地放置的问题相同：每次交换后，明确哪部分已经处理、哪部分仍然未知。

当某个简短技巧看起来很神奇时，用最小反例删掉一个题目条件试试看。知道它什么时候失效，才算真正会用。
`,
};
