// 知识文章：位运算与实用技巧
// 数据格式约定（所有知识文章遵循）：
// - slug: 英文短横线命名，同时是文件名与路由
// - tags: 与题库中题目的 tags 匹配，用于自动关联题目
// - content: Markdown 子集（### 标题、- 列表、``` 代码块、`行内代码`）
export default {
  slug: 'bit-tricks',
  title: '位运算与实用技巧',
  intro: '异或消消乐、lowbit、补码常识，加上原地算法省空间的三种套路',
  tags: ['位运算', '数学', '计数', '模拟'],
  relatedProblems: [41, 75, 136, 169, 189, 287],

  content: `
> 提示：读这篇文章前，最好已经具备这些基础：
> 1. 会写 for 循环、会用数组；
> 2. 听过「二进制」，知道电脑里的数都是一串 0 和 1（不用会手动换算，跟着文章看两遍例子就会）；
> 3. 知道「O(1) 额外空间」的意思：不能开和输入等大的新数组（《时间复杂度入门》讲过）。

楼道声控灯的开关是同一只按钮：按一下亮，再按一下灭。同一盏灯被按了偶数次，最后一定是灭的；只有被按了奇数次的灯还亮着。本文的主角「异或」，玩的就是这套**偶数次抵消**的把戏。

### 是什么

先用大白话。整数在电脑里存成一排 0 和 1（二进制），比如 4 是 100，5 是 101。**位运算**就是直接对这一排 0/1 按位做运算：每一位独立计算，各管各的，速度极快。

为什么说快：按位与、或、异或、移位都是 CPU 一条指令直接支持的，比加减乘除还便宜——不过刷题里它的价值主要不在快，而在**省空间**，往下看例子就懂。

先补 30 秒二进制课（本文的数都很小，看懂这三条就够）：

- 二进制从右往左，每一位依次代表 1、2、4、8、16……（2 的幂）
- 某一位是 1，就把它代表的值加进总和：1101 = 8 + 4 + 0 + 1 = 13
- 本文的数最大只有 7，三位二进制就装得下：**右数第一位代表 1，第二位代表 2，第三位代表 4**

刷题用得最多的是**异或**，写作 \`^\`，规则一句话：**两位相同得 0，不同得 1**——记成「相同关、不同开」。拿 5 ^ 3 演一遍：5 是 101，3 是 011，逐位比——1 和 0 不同得 1，0 和 1 不同得 1，1 和 1 相同得 0，结果 110，也就是 6。

常用位运算符一览：

- \`&\` 按位与：两位都是 1 才得 1
- \`|\` 按位或：两位有一个 1 就得 1
- \`^\` 按位异或：两位不同才得 1（本文主角）
- \`~\` 按位取反：0 变 1、1 变 0（注意它不是负号，详见常见错误）
- \`<<\` / \`>>\` 左移 / 右移：整排 0/1 往左 / 往右挪一位，相当于乘 2 / 整除 2

异或有三条「天规」，后面全靠它们吃饭：

- \`a ^ a = 0\`：同一个数异或两次就没了（相同的位全关）
- \`a ^ 0 = a\`：和 0 异或等于没动
- **交换律 + 结合律**：一串数异或，先后顺序随便换，结果不变

三条合起来得到一个刷屏级结论：**一堆数全部异或起来，出现偶数次的会互相抵消清场，出现奇数次的最后留下**。

换个视角也好记：异或就是「不进位的加法」——1 + 1 本该得 10，但异或不进位，只剩 0。这三条背不熟没关系，往下把例子看完，再回头读一遍就懂了。

### 一个最小例子

题目（136 只出现一次的数字）：数组 \`[4, 1, 2, 1, 2]\` 里，1 和 2 都出现了两次，只有 4 出现一次。要求 O(1) 额外空间把 4 找出来。

先验证前提：1 出现 2 次 ✓，2 出现 2 次 ✓，只有 4 落了单 ✓——前提满足，「偶数次抵消」才玩得起来。

做法：设一个变量 \`ans = 0\`，把每个数依次异或进去，最后 ans 里就是落单那个数。为什么？对照表格慢慢看——把 ans 想成三盏灯（三个二进制位），看它们怎么亮、怎么灭：

| 步骤 | 当前数 x | x 的二进制 | 本步发生的事 | ans（十进制） | ans 的二进制 |
| --- | --- | --- | --- | --- | --- |
| 起点 | — | — | ans = 0，三盏灯全灭（0 和任何数异或都不改变它） | 0 | 000 |
| 1 | 4 | 100 | 高位灯从灭变亮（0 ^ 1 = 1） | 4 | 100 |
| 2 | 1 | 001 | 低位灯从灭变亮，和高位的 4 互不干扰 | 5 | 101 |
| 3 | 2 | 010 | 中间灯从灭变亮 | 7 | 111 |
| 4 | 1 | 001 | 低位灯撞上之前那盏 1：1 ^ 1 = 0，熄了 | 6 | 110 |
| 5 | 2 | 010 | 中间灯撞上之前那盏 1：1 ^ 1 = 0，熄了 | 4 | 100 |

一趟走完，两对的灯互相熄灭，只剩落单的 4 那盏还亮着——**答案就是 ans = 4**。全程只用了 ans 一个变量，没开任何新数组，O(1) 空间达成。

顺便验证一下交换律：把顺序打乱成 [1, 2, 4, 1, 2]，ans 依次是 1 → 3 → 7 → 6 → 4，照样收在 4。建议拿纸笔把这一遍也推完，推完异或就算过关了。

### 模板代码

JavaScript 版本：

\`\`\`
// JavaScript：136 只出现一次的数字
function singleNumber(nums) {
  let ans = 0;              // 起点必须是 0：a ^ 0 = a，不影响任何数
  for (const x of nums) {   // 数组里的数轮流进异或
    ans = ans ^ x;          // 出现偶数次的互相抵消，奇数次的留到最后
  }
  return ans;               // ans 里就是落单的那个数
}
\`\`\`

Python 版本：

\`\`\`
# Python：136 只出现一次的数字
def singleNumber(nums):
    ans = 0                 # 起点必须是 0：a ^ 0 = a，不影响任何数
    for x in nums:          # 数组里的数轮流进异或
        ans = ans ^ x       # 出现偶数次的互相抵消，奇数次的留到最后
    return ans              # ans 里就是落单的那个数
\`\`\`

C++ 版本：

\`\`\`cpp
// C++：136 只出现一次的数字
int singleNumber(vector<int>& nums) {
    int ans = 0;              // 起点必须是 0：a ^ 0 = a，不影响任何数
    for (int x : nums) {      // 数组里的数轮流进异或
        ans = ans ^ x;        // 出现偶数次的互相抵消，奇数次的留到最后
    }
    return ans;               // ans 里就是落单的那个数
}
\`\`\`

三个版本的核心逻辑加起来只有 4 行。背下来之前先回答两个问题：为什么 ans 必须从 0 开始？为什么一遍循环就够了？答得出才算真懂，不是背住。

异或之外，还有三招「小抄」在题库里反复出现，先混个眼熟：

- **\`x & -x\`（lowbit）**：单独取出 x 最低位的那个 1。原理一句话：计算机里负数是「按位取反再加 1」（这叫补码），所以 x 和 -x 只在最低位的 1 那里「撞车」，一「与」就单独抠出来了
- **\`x & (x - 1)\`**：消掉最低位的 1。循环执行能数出二进制里有几个 1；也可以判断 2 的幂（2 的幂的二进制里只有 1 个 1，消完直接变 0）
- **\`x << 1\` / \`x >> 1\`**：乘 2 / 整除 2 的提速写法

以 \`x & (x - 1)\` 为例，拿 x = 12（二进制 1100）演示「数 1 的个数」：每执行一次就消掉一个最低位的 1，循环到 0 为止，循环了几次就有几个 1。

| 循环次数 | 本步的 x | x 的二进制 | x - 1 | 相与之后的新 x | 发生了什么 |
| --- | --- | --- | --- | --- | --- |
| 1 | 12 | 1100 | 11（1011） | 8（1000） | 代表 4 的那位 1 被消掉 |
| 2 | 8 | 1000 | 7（0111） | 0（0000） | 最后剩下的 1 也被消掉 |

循环 2 次收工 → 12 的二进制里有 **2 个 1**。

「判断 2 的幂」就是数 1 的个数是否为 1：8（1000）是，12 不是。一行就能写：\`x > 0 && (x & (x - 1)) === 0\`。

### 亲手跑一跑

跑一遍 [4, 1, 2, 1, 2] 的异或消消乐，每一步把 ans 的十进制和二进制都打印出来，亲眼看灯怎么亮、怎么灭。

小知识：JS 里 \`x.toString(2)\` 是把整数转成二进制字符串；Python 里 \`format(x, '03b')\` 的意思是「转成二进制、不足 3 位就在前面补 0」。

JavaScript：

\`\`\`run-js#xor-walkthrough
const nums = [4, 1, 2, 1, 2];
let ans = 0;
const bin = (x) => x.toString(2).padStart(3, '0');  // 转成 3 位二进制字符串
console.log('起点：ans = ' + ans + '（二进制 ' + bin(ans) + '）');
for (const x of nums) {
  const before = ans;
  ans = ans ^ x;
  console.log('异或 ' + x + '（' + bin(x) + '）：ans 从 ' + before + '（' + bin(before) + '）变成 ' + ans + '（' + bin(ans) + '）');
}
console.log('只出现一次的数是：' + ans);
\`\`\`

Python：

\`\`\`run-py#xor-walkthrough
nums = [4, 1, 2, 1, 2]
ans = 0
def bin3(x):
    return format(x, '03b')      # 转成 3 位二进制字符串
print('起点：ans = ' + str(ans) + '（二进制 ' + bin3(ans) + '）')
for x in nums:
    before = ans
    ans = ans ^ x
    print('异或 ' + str(x) + '（' + bin3(x) + '）：ans 从 ' + str(before) + '（' + bin3(before) + '）变成 ' + str(ans) + '（' + bin3(ans) + '）')
print('只出现一次的数是：' + str(ans))
\`\`\`

C++：

\`\`\`run-cpp#xor-walkthrough
#include <iostream>
#include <string>
#include <vector>
using namespace std;

string bin3(int x) {             // 转成 3 位二进制字符串
    string s = "";
    for (int i = 2; i >= 0; i--) {
        s += ((x >> i) & 1) ? '1' : '0';
    }
    return s;
}

int main() {
    vector<int> nums = {4, 1, 2, 1, 2};
    int ans = 0;
    cout << "起点：ans = " << ans << "（二进制 " << bin3(ans) << "）" << endl;
    for (int x : nums) {
        int before = ans;
        ans = ans ^ x;
        cout << "异或 " << x << "（" << bin3(x) << "）：ans 从 " << before << "（" << bin3(before) << "）变成 " << ans << "（" << bin3(ans) << "）" << endl;
    }
    cout << "只出现一次的数是：" << ans << endl;
    return 0;
}
\`\`\`

三个版本输出一致：

\`\`\`
起点：ans = 0（二进制 000）
异或 4（100）：ans 从 0（000）变成 4（100）
异或 1（001）：ans 从 4（100）变成 5（101）
异或 2（010）：ans 从 5（101）变成 7（111）
异或 1（001）：ans 从 7（111）变成 6（110）
异或 2（010）：ans 从 6（110）变成 4（100）
只出现一次的数是：4
\`\`\`

看第 4、5 行：第二个 1 和第二个 2 进来时，ans 对应的那盏灯正好从亮变灭——这就是「成对抵消」的实况。再跑一个实验：把 nums 改成 [7, 3, 5, 3, 5]（落单的是 7），最后一行输出应该变成 7。

输出里的十进制列也顺手看一眼：ans 先涨到 7，再被一个个减回 4——和「灯亮灯灭」是同一件事的两种看法。

### 直觉想法错在哪

最直觉的想法：「那就数一数每个数出现几次呗」——开个哈希表，键是数、值是次数，最后找次数为 1 的。思路完全正确，但要开一张和数组等大的表，O(n) 额外空间，题目要求 O(1) 就过不了关。第二个直觉：先排序，再相邻两个两个比对——排序本身就要 O(n log n)，还会改动原数组。

三种做法摆在一起看：

- 哈希表计数：时间 O(n)、额外空间 O(n)——最好懂，但空间超标
- 先排序再相邻比对：时间 O(n log n)，还得改动或复制原数组——两头都不占
- 异或消消乐：时间 O(n)、额外空间 O(1)——本题的最优解

值得记住的不是「异或很巧」，而是**题目白送了一个性质——其他数都出现偶数次**。有偶数才有抵消；下次再看到「成对出现找落单」，条件反射就该想到它。如果题目不卡空间，哈希表写法才是更好懂、更不容易错的那个选择。

### 常见错误

- **ans 不初始化，或初始化成 1**：必须从 0 开始，因为只有 0 异或任何数都不改变它；初始成 1，等于平白混进来一个 1。容易犯是因为习惯觉得「初始值随便给，反正后面会覆盖」。
- **位运算裸写不加括号**：\`a & b === 0\` 会先算 \`b === 0\`（JS、Python 和 C++ 里比较运算的优先级都高于 &），判断必须写成 \`(a & b) === 0\`。容易犯是因为数学里「先乘除后加减」的直觉，让人以为 & 也粘得紧。
- **把 \`~x\` 当成相反数**：\`~\` 是按位取反，\`~x\` 等于 \`-x - 1\`，所以 \`~5\` 是 -6 不是 -5。容易犯是因为名字里带「反」，第一眼以为就是负号。
- **以为异或消消乐万能**：它的前提是「其他数都出现偶数次」。如果其他数都出现 3 次，异或完不会清场，结果全错（那要换「按位统计次数」的做法）。容易犯是因为只背了结论、没记条件。
- **JS 里对超大整数做位运算**：JS 位运算前会把数截断成 32 位有符号整数，超过 2 的 31 次方的数会被截坏。平时算数没感觉，到位运算才暴露。

### 什么时候用

- **「每个数都出现偶数次（通常是两次），只有一个落单」**——异或消消乐（136）
- **要求 O(1) 额外空间**，连哈希表都不让开
- **「数二进制里 1 的个数」「判断 2 的幂」**——用上面的 lowbit 两招
- **状态只有两种、反复切换**（亮 / 灭、选 / 不选），天然适合异或
- **附加条件怪题**：「交换两个数不用临时变量」「不用加减做加法」这类面试附加题，也都靠位运算
- 同宗技巧：**原地算法**——不让开新数组时，把信息编码进数组本身（下标当哈希、正负号当标记、交换位置），代表题 189 轮转数组（三次反转）、75 颜色分类（三指针）、41 缺失的第一个正数（原地哈希）

一句话总结：位运算不是炫技，是被「空间限制」或「成对结构」逼出来的趁手工具。

### 练习路径

按这个顺序刷，每题的预期收获写在后面：

- **136 只出现一次的数字**：本文例子，把异或消消乐完整跑通一遍
- **169 多数元素**：出现次数过半的数。可以用位运算按位统计，更推荐顺便学 Boyer-Moore 投票法——「两两配对互相抵消」，和异或是同一种思想
- **189 轮转数组**：不开新数组的「三次反转」原地技巧：先整体反转，再分段反转
- **75 颜色分类**：三指针原地分区，从此不怕「只能用一个数组」
- **41 缺失的第一个正数**：把数组本身当哈希表用（原地哈希），Hard，原地思想的综合考

这五题刷完，再碰到「只给你常数空间」的要求，第一反应就不会是慌了。
`,
};
