// 知识文章：时间复杂度入门
// 数据格式约定（所有知识文章遵循）：
// - slug: 英文短横线命名，同时是文件名与路由
// - tags: 与题库中题目的 tags 匹配，用于自动关联题目
// - relatedProblems（可选）: 显式指定关联题目 id，优先于 tags 匹配
// - content: Markdown 子集（### 标题、- 列表、``` 代码块、`行内代码`）
export default {
  slug: 'complexity',
  title: '时间复杂度入门',
  intro: '看懂大 O 记号，学会估算自己的代码会不会超时',
  tags: ['数学', '模拟'],
  relatedProblems: [1, 35, 53, 56, 78],

  content: `
> 提示：本篇是知识板块的第一篇，零基础可直接读。只需提前知道两件事：程序是一行一行往下执行的；for 循环会把里面的代码重复执行很多遍。

给做菜估时间不用掐秒表：一盘炒 10 分钟，8 盘大约 80 分钟，客人翻倍，时间就翻倍。时间复杂度就是给代码做同样的估算——不管实际跑几毫秒，只看输入翻倍时，要干的活会多几倍。

### 是什么

先抛开术语，用大白话说：**时间复杂度描述的是，输入规模 n 变大时，代码会变慢多快。**

- n 指输入规模：数组有多长、字符串有多少字符。
- 不看「跑了几毫秒」（那取决于电脑好坏和语言快慢），只看执行次数随 n 的**增长趋势**。
- 这个趋势写成 O(···)，读作「大 O」，叫**大 O 记号**。

术语对照表：

| 大白话 | 术语读法 | 典型代码 |
| --- | --- | --- |
| n 翻倍，时间不变 | O(1) 常数 | 按下标取 nums[0] |
| n 翻倍，只多一步 | O(log n) 对数 | 有序数组二分查找 |
| n 翻倍，时间也翻倍 | O(n) 线性 | 把数组从头看一遍 |
| n 翻倍，时间变 4 倍 | O(n^2) 平方 | 两层循环枚举所有数对 |
| n 加 1，时间翻倍 | O(2^n) 指数 | 枚举所有子集 |

刷题最常见的几个量级，各配一个画面：

- **O(1)**：次数和 n 无关，比如 \`nums[i]\` 取值、哈希表查一次。
- **O(log n)**：每步把问题砍一半，典型是二分查找；n 到十亿也只要 30 步上下。
- **O(n)**：每个元素看固定的几遍，比如 53. 最大子数组和一趟扫完。
- **O(n log n)**：排序的量级，「先排序再处理」的题都带着它，比如 56. 合并区间。
- **O(n^2)**：两层循环各扫一遍 n，比如 1. 两数之和的暴力枚举；n = 10^5 就是 100 亿次，必超时。
- **O(2^n)**：每个元素「选或不选」各分一支，比如 78. 子集；这类题的数据范围必然很小（n ≤ 20 上下），暴力本来就够用。

大 O 化简的两条规则，遇到复杂式子用来收尾：

- **忽略常数系数**：O(2n) 就写 O(n)。每盘菜炒 10 分钟还是 20 分钟，不改变「时间随盘数成正比」这个事实。
- **只留长得最快的项**：O(n^2 + n) 就写 O(n^2)。n 很大时，小项的影响可以忽略。

### 一个最小例子

在排好序的数组 nums = [1, 3, 5, 7, 9, 11, 13, 15]（n = 8）里找 13，亲手数一数两种找法各走几步。

**找法一：从头一个个看 —— O(n) 线性查找**

| 步骤 | 查看下标 | 看到的值 | 本步判断 | 本步动作 |
| --- | --- | --- | --- | --- |
| 1 | 0 | 1 | 不是 13 | 看下一位 |
| 2 | 1 | 3 | 不是 13 | 看下一位 |
| 3 | 2 | 5 | 不是 13 | 看下一位 |
| 4 | 3 | 7 | 不是 13 | 看下一位 |
| 5 | 4 | 9 | 不是 13 | 看下一位 |
| 6 | 5 | 11 | 不是 13 | 看下一位 |
| 7 | 6 | 13 | 找到了 | 结束，共 7 步 |

最倒霉的情况（目标在最后一位，或根本不在）得看满 8 位——n 是多少，最多就走 n 步，所以叫 O(n)。

**找法二：每次从中间翻开，扔掉一半 —— O(log n) 二分查找**

数组是排好序的，翻开中点比一比，就知道答案在左半边还是右半边：

| 步骤 | 剩余范围 left~right | 中点下标 mid | nums[mid] | 本步判断 | 本步动作 |
| --- | --- | --- | --- | --- | --- |
| 1 | 0~7 | 3 | 7 | 7 < 13，去右半 | 范围缩到 4~7 |
| 2 | 4~7 | 5 | 11 | 11 < 13，去右半 | 范围缩到 6~7 |
| 3 | 6~7 | 6 | 13 | 找到了 | 结束，共 3 步 |

每翻一次，范围减半：8 到 4 到 2 到 1。n 连除几次 2 才到 1，最多就翻几次——所以叫 O(log n)。

把规模放大，两种找法的差距急剧拉开：

| 数组长度 n | O(n) 大约步数 | O(log n) 大约步数 |
| --- | --- | --- |
| 8 | 8 | 3 |
| 16 | 16 | 4 |
| 1024 | 1024 | 10 |
| 一百万 | 一百万 | 20 |
| 十亿 | 十亿 | 30 |

> 重点：n 小的时候，什么写法都差不多；n 一大，复杂度差一个量级就是「秒出结果」和「超时报错」的区别。刷题时问「这个解法会不会超时」，本质就是一道估算题。

### 模板代码

估算复杂度的基本动作是「看循环把操作重复几遍」。三种最常见的模式，先认个脸熟：

\`\`\`
// ===== JavaScript =====
// 模式一：一层循环跑满 n —— O(n)
for (let i = 0; i < n; i++) {        // i 从 0 数到 n-1，循环体执行 n 次
  // 每次只做固定几件小事，比如比较一次
}
// 模式二：两层循环都跑满 n —— O(n^2)
for (let i = 0; i < n; i++) {        // 外层跑 n 次
  for (let j = 0; j < n; j++) {      // 每进一次外层，内层再跑 n 次
    // 循环体总共执行 n * n 次
  }
}
// 模式三：规模每轮减半 —— O(log n)
for (let size = n; size > 1; size = size / 2) {  // n -> n/2 -> n/4 ... 只循环 log n 轮
  // 二分查找就长这个形状
}
\`\`\`

\`\`\`
# ===== Python =====
# 模式一：一层循环跑满 n —— O(n)
for i in range(n):                   # i 从 0 数到 n-1，循环体执行 n 次
    pass                             # 每次只做固定几件小事，比如比较一次
# 模式二：两层循环都跑满 n —— O(n^2)
for i in range(n):                   # 外层跑 n 次
    for j in range(n):               # 每进一次外层，内层再跑 n 次
        pass                         # 循环体总共执行 n * n 次
# 模式三：规模每轮减半 —— O(log n)
size = n
while size > 1:                      # n -> n/2 -> n/4 ... 只循环 log n 轮
    size = size // 2                 # 二分查找就长这个形状
\`\`\`

\`\`\`cpp
// ===== C++ =====
// 模式一：一层循环跑满 n —— O(n)
for (int i = 0; i < n; i++) {        // i 从 0 数到 n-1，循环体执行 n 次
    // 每次只做固定几件小事，比如比较一次
}
// 模式二：两层循环都跑满 n —— O(n^2)
for (int i = 0; i < n; i++) {        // 外层跑 n 次
    for (int j = 0; j < n; j++) {    // 每进一次外层，内层再跑 n 次
        // 循环体总共执行 n * n 次
    }
}
// 模式三：规模每轮减半 —— O(log n)
for (int size = n; size > 1; size /= 2) {  // n -> n/2 -> n/4 ... 只循环 log n 轮
    // 二分查找就长这个形状
}
\`\`\`

库函数不是免费的，估算时要把它们算进去：

- sort 排序一次是 O(n log n)，还把它写进循环，整体就是 O(n^2 log n)
- 数组的 includes / indexOf、Python 的 x in list 都是 O(n)；Map / dict / set 的读写才是 O(1)

### 亲手跑一跑

别只看理论，让程序自己数一数：同样的 n = 16，三种模式各循环几次。

\`\`\`run-js#complexity-growth
const n = 16;
let a = 0;
for (let i = 1; i <= n; i++) a++;    // O(n)：每个数都碰一次
console.log('O(n): 循环了 ' + a + ' 次');
let b = 0;
for (let size = n; size > 1; size = size / 2) {  // O(log n)：每轮规模减半
  b++;
  console.log('O(log n): 第 ' + b + ' 轮, 规模 ' + size + ' -> ' + (size / 2));
}
console.log('O(log n): 一共只循环了 ' + b + ' 次');
let c = 0;
for (let i = 0; i < n; i++) {        // O(n^2)：每个数都和所有数配一次对
  for (let j = 0; j < n; j++) c++;
}
console.log('O(n^2): 循环了 ' + c + ' 次');
\`\`\`

\`\`\`run-py#complexity-growth
n = 16
a = 0
for i in range(1, n + 1):            # O(n)：每个数都碰一次
    a += 1
print('O(n): 循环了 ' + str(a) + ' 次')
b = 0
size = n
while size > 1:                      # O(log n)：每轮规模减半
    b += 1
    print('O(log n): 第 ' + str(b) + ' 轮, 规模 ' + str(size) + ' -> ' + str(size // 2))
    size = size // 2
print('O(log n): 一共只循环了 ' + str(b) + ' 次')
c = 0
for i in range(n):                   # O(n^2)：每个数都和所有数配一次对
    for j in range(n):
        c += 1
print('O(n^2): 循环了 ' + str(c) + ' 次')
\`\`\`

\`\`\`run-cpp#complexity-growth
#include <iostream>
using namespace std;

int main() {
    int n = 16;
    int a = 0;
    for (int i = 1; i <= n; i++) a++;  // O(n)：每个数都碰一次
    cout << "O(n): 循环了 " << a << " 次" << endl;
    int b = 0;
    for (int size = n; size > 1; size /= 2) {  // O(log n)：每轮规模减半
        b++;
        cout << "O(log n): 第 " << b << " 轮, 规模 " << size << " -> " << (size / 2) << endl;
    }
    cout << "O(log n): 一共只循环了 " << b << " 次" << endl;
    int c = 0;
    for (int i = 0; i < n; i++) {      // O(n^2)：每个数都和所有数配一次对
        for (int j = 0; j < n; j++) c++;
    }
    cout << "O(n^2): 循环了 " << c << " 次" << endl;
    return 0;
}
\`\`\`

预期输出（三种语言逐字一致）：

\`\`\`
O(n): 循环了 16 次
O(log n): 第 1 轮, 规模 16 -> 8
O(log n): 第 2 轮, 规模 8 -> 4
O(log n): 第 3 轮, 规模 4 -> 2
O(log n): 第 4 轮, 规模 2 -> 1
O(log n): 一共只循环了 4 次
O(n^2): 循环了 256 次
\`\`\`

同样的 n，O(n) 干 16 次活、O(log n) 只干 4 次，而 O(n^2) 已经干了 256 次。n 越大，差距越夸张。

### 直觉想法错在哪

零基础最常见的直觉：**「先把所有可能都列出来再说」**。比如两数之和，两层循环枚举每一对数，几行就能写出来。但代价藏在数据规模里：n = 10 万时，数对约有 50 亿对；评测机一秒大约只干 1 亿次简单操作，光枚举就要将近一分钟，远超时限。

第二个自带错觉的想法：**「跑一下看看快不快」**。自己手测的数据往往只有几十个数，O(n^2) 也飞快；评测机却专挑最大规模的数据卡你。复杂度估算的价值就在于——不用真跑，看一眼数据范围就能预判生死。

### 常见错误

- **把 sort 当免费午餐**：以为排序「反正很快」。排序一次就是 O(n log n)，写进循环里更会悄悄变成 O(n^2 log n)。
- **循环里查存在**：includes / indexOf / x in list 查一次是 O(n)，套在 for 里就退化成 O(n^2)。判断「在不在」要换哈希集合（Set / set）。
- **只会数循环层数**：滑动窗口、双指针看着是 while 套 for，但指针从头到尾只走一遍，总次数是 2n，仍是 O(n)。要数的是「总执行次数」，不是嵌套层数。
- **把递归当小事**：每层都分叉的递归会爆炸，比如每步生两支、共 n 层，就是 O(2^n) 次调用——这也是 70. 爬楼梯不能裸递归的原因。
- **忘了递归也占空间**：空间复杂度的算法相同，递归调用栈最深 n 层就是 O(n) 空间，不是 O(1)。

### 什么时候用

每拿到一道题，先看 n 的数据范围，反推能接受的复杂度上限。下面只是按约 10^7 级简单操作量给出的保守经验；语言、常数、时限都会改变边界：

- n ≤ 10：O(n · n!) 才可能接受，全排列类直接暴力枚举
- n ≤ 20：O(2^n) 常见；到 25 已有约 3300 万个状态，必须看常数和时限
- n ≤ 200：O(n^3) 约 800 万次，通常可行
- n ≤ 2000：O(n^2) 约 400 万次，通常可行
- n ≈ 10^4：O(n^2) 已达 1 亿次，通常不该默认能过，应优先 O(n log n) 或 O(n)
- n ≤ 10^5：通常需要 O(n log n) 以内，排序、二分、堆是主力
- n ≤ 10^6：通常需要接近 O(n)，还要留意常数与内存
- n ≥ 10^8：连 O(n) 扫描都可能超时，只能结合时限考虑 O(log n)、O(1) 或避免完整处理输入

正确的做题顺序：先看数据范围圈定量级，再在量级内找算法——而不是闷头写完暴力，再回头想怎么优化。

### 练习路径

- 35. 搜索插入位置：亲手写一遍二分查找，体会「每次扔掉一半」的 O(log n)
- 53. 最大子数组和：一次遍历出答案，体会 O(n) 一趟扫完的手感
- 56. 合并区间：体会「先排序」带来的 O(n log n) 打底
- 1. 两数之和：先写 O(n^2) 的暴力，再亲手优化到 O(n)，完整走一遍「估算 → 超时 → 优化」
`,
};
