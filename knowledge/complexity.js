// 知识文章：时间与空间复杂度
export default {
  "slug": "complexity",
  "title": "时间与空间复杂度",
  "intro": "输入变大后，代码要多做多少工作？从循环次数开始估算。",
  "tags": ["时间复杂度","空间复杂度"],
  "relatedProblems": [
    {
      "id": 1,
      "stage": "core",
      "reason": "比较枚举与查表"
    },
    {
      "id": 35,
      "stage": "core",
      "reason": "观察范围减半"
    },
    {
      "id": 78,
      "stage": "practice",
      "reason": "计算输出本身的规模"
    }
  ],
  content: `
学算法时，你经常会写出两个都能算对的程序。输入只有十个数时，它们看起来没什么区别；输入变成十万个数，其中一个却迟迟跑不完。时间复杂度就是用来讨论这种差别的。

这篇只需要你会写循环。读完后，你应该能解释一段代码为什么是 O(n)、O(n²) 或 O(log n)，并知道这种估算有哪些限制。

### 先数执行次数

假设要在 n 个数里找最大值。你可以记住目前最大的数，然后从左到右看一遍。每读一个数只做一次比较，总共大约 n 次。我们说时间复杂度是 O(n)。这里的 n 是输入规模，不是某个具体的数。

如果要枚举数组中所有不同位置的数对，可以固定第一个位置，再遍历它后面的位置。比较次数是 (n−1)+(n−2)+…+1，也就是 n(n−1)/2。它随 n 的平方增长，记作 O(n²)。

| 输入长度 | 遍历一遍 | 枚举所有数对 |
| --- | --- | --- |
| 100 | 100 次 | 4,950 对 |
| 10,000 | 10,000 次 | 49,995,000 对 |
| 100,000 | 100,000 次 | 4,999,950,000 对 |

重点是输入扩大后，工作量如何增长。O(n²) 不表示程序恰好执行 n² 条指令，也不表示一定比 O(n) 慢：输入很小时，常数开销同样重要。

### 为什么不写成 O(3n+5)

假设代码先遍历两次数组，再遍历一次，最后做五次固定操作。执行量约为 3n+5。讨论增长速度时，常数倍和低阶项不会改变它属于线性增长这一点，所以通常简写为 O(n)。

严格说，大 O 表示渐近上界；刷题讲解通常用它描述所分析情形下的主要增长量。看到别人写 O(n)，还应留意他说的是最坏、平均还是均摊情况。

### 循环有几层，不等于复杂度有几次方

两个前后相接的循环各执行 n 次，总共 2n 次，是 O(n)。

两个嵌套循环，如果内层每次都完整执行 n 次，才会累积成 n² 次。滑动窗口虽然也常写成 for 里面套 while，但左边界在整个算法中只向右移动，最多移动 n 次；总工作量仍然可以是 O(n)。

另一个常见情况是二分查找。每一步把候选范围减半：n、n/2、n/4……直到只剩一个位置。减半的次数约为 log₂n，所以是 O(log n)。十万个位置，经过约 17 次减半就只剩常数个位置。

别漏掉函数调用的代价。例如循环里每次都对整个数组排序、复制或查找，即使只写了一层循环，也可能很慢。

### 空间复杂度算哪些东西

通常讨论的是额外空间：输入本身不算，算法另外申请的数组、哈希表以及递归调用栈要算。

找最大值只需要保存一个变量，额外空间是 O(1)。把所有数复制到新数组需要 O(n)。树的深度优先遍历若递归深度为 h，调用栈就是 O(h)；树退化成链时，h 可以等于节点数 n。

返回所有子集时，光答案就可能占 O(n·2ⁿ) 空间。要说明是否把输出计入空间复杂度，不能一边生成大量答案，一边只说“用了一个 path，所以是 O(n)”。

### 用题目范围检查思路

没有一个适用于所有语言和机器的“每秒最多执行多少次”标准。可以先做量级估算，再实际运行：

- n 是十万时，O(n²) 通常需要重新考虑；O(n) 或 O(n log n) 更值得尝试。
- n 只有十几时，枚举子集可能可行，因为子集数是 2ⁿ。
- 排列数是 n!，比 2ⁿ 增长更快，规模稍大就很难全部枚举。
- 动态规划还可能依赖金额、容量等数值。例如零钱兑换的 O(n·amount) 同时取决于硬币种数和目标金额。

这些是筛选思路的依据，不是通过判题的保证。代码常数、内存限制和输入分布也会影响结果。

### 自己检查一次

考虑“把数组排序，再遍历一次”。比较排序一般按 O(n log n) 分析，加上 O(n)，整体仍写成 O(n log n)。但额外空间要看你使用的排序实现，不能只凭这一行调用就断言是 O(1)。

下面的例子数的是数对，不依赖机器速度。输入长度为 4 时，运行前先写下你的预测。

\`\`\`run-py#complexity-v2-demo
def count_pairs(nums):
    count = 0
    for i in range(len(nums)):
        for j in range(i + 1, len(nums)):
            count += 1
    return count

print(count_pairs([10, 20, 30, 40]))
\`\`\`

\`\`\`run-js#complexity-v2-demo
function countPairs(nums) {
  let count = 0;
  for (let i = 0; i < nums.length; i++) {
    for (let j = i + 1; j < nums.length; j++) count++;
  }
  return count;
}
console.log(countPairs([10, 20, 30, 40]));
\`\`\`

\`\`\`run-cpp#complexity-v2-demo
#include <iostream>
#include <vector>
using namespace std;
long long countPairs(const vector<int>& nums) {
    long long count = 0;
    for (int i = 0; i < (int)nums.size(); ++i)
        for (int j = i + 1; j < (int)nums.size(); ++j) ++count;
    return count;
}
int main() { cout << countPairs({10, 20, 30, 40}) << '\\n'; }
\`\`\`

### 接下来做什么

先做第 1 题“两数之和”，比较两层枚举与哈希表的工作量；再做第 35 题“搜索插入位置”，观察二分怎样减少候选范围。学完回溯后回头看第 78 题“子集”：那时你会看到，有些问题慢是因为答案本来就多。

检查自己是否理解：如果输入翻倍，线性算法工作量约翻两倍，平方算法约翻四倍。二分查找增加的通常只是一轮判断。
`,
};
