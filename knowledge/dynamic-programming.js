// 知识文章：动态规划入门
// 数据格式约定（所有知识文章遵循）：
// - slug: 英文短横线命名，同时是文件名与路由
// - tags: 与题库中题目的 tags 匹配，用于自动关联题目
// - content: Markdown 子集（### 标题、- 列表、``` 代码块、`行内代码`）
export default {
  slug: 'dynamic-programming',
  title: '动态规划入门',
  intro: '把大问题拆成小问题、存下答案反复用：状态、转移、初始值、遍历顺序四要素',
  tags: ['动态规划', '记忆化搜索', '数组', '字符串'],
  relatedProblems: [53, 62, 70, 72, 198, 300, 322, 416, 1143],

  content: `
> 提示：读这篇文章前，最好已经具备这些基础：
> 1. 知道什么是递归（《二叉树与递归》讲过）；
> 2. 会看 O(n)、O(2ⁿ) 这类复杂度记号（《时间复杂度入门》讲过），本文要拿它俩做对比；
> 3. 不必须，但更有感觉：读过《回溯算法》——动态规划常被理解成「暴力穷举的省时间版」。

要数「爬 45 级台阶有多少种走法」，每算一层都要用到前两层的答案，而前两层自己也是这么算出来的——同一个答案会被翻来覆去算几十次。聪明人的做法：每算出一个答案就记在手背上，下次要用先看手背，有就抄，没有才算。

**动态规划（DP）就是这个「手背记忆法」：把算过的答案存进格子，一格只算一次，绝不重复劳动。**

### 是什么

先用大白话说。有些题目能拆成「更小的同款问题」：比如「爬到第 5 阶的走法数」=「爬到第 4 阶的走法数」+「爬到第 3 阶的走法数」。如果直接递归去拆，同一个小问题会被拆出来算无数次，规模一大就慢到离谱。

动态规划的做法：给每个小问题准备一格空位，排成一行表格（就是一个数组）；从最小的问题开始，一格一格往后填，**每格的答案只算一次，算好就永远写在格子里**，后面谁要用直接抄，绝不再算第二遍。

名字唬人，其实 DP 既不「动态」也不玄乎——你可以直接把它读作「填表法」。

做题时按「四要素」逐项确认，缺一不可：

- **状态定义**：每一格记的是什么，写作 \`dp[i]\`。比如「dp[i] = 到第 i 阶的走法数」
- **转移方程**：这一格怎么用前面几格算出来。比如 \`dp[i] = dp[i - 1] + dp[i - 2]\`——最后一步要么迈 1 级、要么迈 2 级，两种来源相加
- **初始值**：最小的几格直接手填（比如 dp[1] = 1）。初始值填错，整张表全错
- **遍历顺序**：被依赖的格子必须先算。爬楼梯里从左往右填就行

还有两个「能不能用 DP」的判断信号：**小问题会被重复遇到**（值得记下来，否则白记），以及**大问题的答案能由小问题拼出来**（否则格子之间没法互相抄）。

### 一个最小例子

爬楼梯（70 题）：一共 5 阶台阶，每次只能迈 1 级或 2 级，问爬到楼顶有几种走法。

为什么能拆：你迈上第 i 阶的**最后一步**，只可能是两种情况——从第 i - 1 阶迈 1 级上来，或者从第 i - 2 阶迈 2 级上来。没有第三种可能，所以：

**dp[i] = dp[i - 1] + dp[i - 2]**（到第 i 阶的走法数 = 两种「最后一步」的来源相加）

注意，整个思考过程没有用一行代码，全是草稿纸上的推理——DP 题八成的功夫花在动笔之前。下面是整张表的填写过程，每一格用到的都是**左边已经填好的格子**：

| 格子 | 含义 | 它由哪两格而来 | 计算过程 | dp 值 |
| --- | --- | --- | --- | --- |
| dp[1] | 到第 1 阶的走法数 | 初始值：只有「迈 1 级」一种走法 | 直接填 | 1 |
| dp[2] | 到第 2 阶的走法数 | 初始值：1+1 两小步，或一步迈 2 级 | 直接填 | 2 |
| dp[3] | 到第 3 阶的走法数 | dp[2] + dp[1] | 2 + 1 | 3 |
| dp[4] | 到第 4 阶的走法数 | dp[3] + dp[2] | 3 + 2 | 5 |
| dp[5] | 到第 5 阶的走法数 | dp[4] + dp[3] | 5 + 3 | 8 |

答案就是最后一格：**8 种走法**。不放心可以验算 dp[3] = 3：走法有（1,1,1）、（1,2）、（2,1)，正好 3 种，表格没有骗人。

换个花色再看一眼——198 打家劫舍：一排 5 户人家，存款依次是 [2, 7, 9, 3, 1]，相邻两家不能都偷（会触发警报），问最多能偷多少。

状态定义：dp[i] = 只考虑前 i 家时，最多能偷到的钱。面对第 i 家只有两个选择：**偷它**（那第 i - 1 家就不能碰，总数是 dp[i - 2] + 这家的钱）或者**不偷**（总数保持 dp[i - 1]）。所以 \`dp[i] = max(dp[i - 1], dp[i - 2] + 这家的钱)\`。

| 格子 | 面对的局面 | 怎么算 | dp 值 |
| --- | --- | --- | --- |
| dp[1] | 第 1 家（存款 2） | 初始值：没得选，偷它 | 2 |
| dp[2] | 前 2 家（2、7） | 初始值：两家相邻不能都偷，偷多的 | 7 |
| dp[3] | 加入第 3 家（9） | max(dp[2] = 7, dp[1] + 9 = 11)，偷第 3 家更赚 | 11 |
| dp[4] | 加入第 4 家（3） | max(dp[3] = 11, dp[2] + 3 = 10)，不偷第 4 家 | 11 |
| dp[5] | 加入第 5 家（1） | max(dp[4] = 11, dp[3] + 1 = 12)，偷第 5 家更赚 | 12 |

答案是 12——正是第 1、3、5 家（2 + 9 + 1）。体会一下：我们从头到尾没有枚举「偷哪几家」的全部组合（那有 2⁵ = 32 种），只是填了 5 个格子。同一个「四要素」清单，换一道题照样走得通。

再体会一点：如果把「相邻不能都偷」的限制去掉，每家都偷、直接求和就完事，根本不需要 DP——限制条件本身，正是递推关系的来源。

### 模板代码

JavaScript 版本：

\`\`\`
// JavaScript：爬楼梯（LeetCode 70）
function climbStairs(n) {
  if (n <= 2) return n;                // 1 阶或 2 阶时答案就是 n，直接返回
  const dp = new Array(n + 1).fill(0); // 开 n+1 格，让下标 i 直接对应第 i 阶
  dp[1] = 1;                           // 初始值①：到第 1 阶只有 1 种走法
  dp[2] = 2;                           // 初始值②：到第 2 阶有 2 种走法
  for (let i = 3; i <= n; i++) {       // 从小到大填表：后面只依赖前面
    dp[i] = dp[i - 1] + dp[i - 2];     // 转移方程：最后一步迈 1 级 or 迈 2 级
  }
  return dp[n];                        // 答案存在最后一格
}
\`\`\`

Python 版本：

\`\`\`
# Python：爬楼梯（LeetCode 70）
def climbStairs(n):
    if n <= 2:
        return n                       # 1 阶或 2 阶时答案就是 n，直接返回
    dp = [0] * (n + 1)                 # 开 n+1 格，让下标 i 直接对应第 i 阶
    dp[1], dp[2] = 1, 2                # 初始值：1 阶 1 种走法，2 阶 2 种走法
    for i in range(3, n + 1):          # 从小到大填表：后面只依赖前面
        dp[i] = dp[i - 1] + dp[i - 2]  # 转移方程：最后一步迈 1 级 or 迈 2 级
    return dp[n]                       # 答案存在最后一格
\`\`\`

C++ 版本：

\`\`\`cpp
// C++：爬楼梯（LeetCode 70）
int climbStairs(int n) {
    if (n <= 2) return n;              // 1 阶或 2 阶时答案就是 n，直接返回
    vector<int> dp(n + 1, 0);          // 开 n+1 格，让下标 i 直接对应第 i 阶
    dp[1] = 1;                         // 初始值①：到第 1 阶只有 1 种走法
    dp[2] = 2;                         // 初始值②：到第 2 阶有 2 种走法
    for (int i = 3; i <= n; i++) {     // 从小到大填表：后面只依赖前面
        dp[i] = dp[i - 1] + dp[i - 2]; // 转移方程：最后一步迈 1 级 or 迈 2 级
    }
    return dp[n];                      // 答案存在最后一格
}
\`\`\`

两个细节值得记住：

- 数组开 \`n + 1\` 而不是 \`n\`，是为了让下标 i 正好对应第 i 阶，少一层脑内换算，\`dp[0]\` 空着不用
- 每格只用到左边两格，所以也能用两个变量「滚动」着代替整个数组，把空间从 O(n) 压到 O(1)——先把数组版写对，再考虑这种优化
- 如果你更习惯「从大问题往下拆」的递归写法，可以给递归加个「备忘录」（算过的 n 直接查表返回），效果一样，这叫**记忆化搜索**，和本文的填表法完全等价

### 亲手跑一跑

用 n = 5 跑一遍完整的填表过程，把每一格的计算都打印出来。

顺便观察一个细节：for 循环从 3 开始，因为 dp[1]、dp[2] 已经手填好了——如果从 0 开始，反而会去访问不存在的格子。

JavaScript：

\`\`\`run-js#climbing-stairs-table
function climbStairs(n) {
  const dp = new Array(n + 1).fill(0);
  dp[1] = 1;
  dp[2] = 2;
  console.log('dp[1] = 1（初始值：只能迈 1 级，1 种走法）');
  console.log('dp[2] = 2（初始值：1+1 或直接迈 2 级，2 种走法）');
  for (let i = 3; i <= n; i++) {
    dp[i] = dp[i - 1] + dp[i - 2];
    console.log('dp[' + i + '] = dp[' + (i - 1) + '] + dp[' + (i - 2) + '] = ' + dp[i - 1] + ' + ' + dp[i - 2] + ' = ' + dp[i]);
  }
  return dp[n];
}
const ans = climbStairs(5);
console.log('爬到第 5 阶共有 ' + ans + ' 种走法');
\`\`\`

Python：

\`\`\`run-py#climbing-stairs-table
def climbStairs(n):
    dp = [0] * (n + 1)
    dp[1], dp[2] = 1, 2
    print('dp[1] = 1（初始值：只能迈 1 级，1 种走法）')
    print('dp[2] = 2（初始值：1+1 或直接迈 2 级，2 种走法）')
    for i in range(3, n + 1):
        dp[i] = dp[i - 1] + dp[i - 2]
        print('dp[' + str(i) + '] = dp[' + str(i - 1) + '] + dp[' + str(i - 2) + '] = ' + str(dp[i - 1]) + ' + ' + str(dp[i - 2]) + ' = ' + str(dp[i]))
    return dp[n]
ans = climbStairs(5)
print('爬到第 5 阶共有 ' + str(ans) + ' 种走法')
\`\`\`

C++：

\`\`\`run-cpp#climbing-stairs-table
#include <iostream>
#include <vector>
using namespace std;

int climbStairs(int n) {
    vector<int> dp(n + 1, 0);
    dp[1] = 1;
    dp[2] = 2;
    cout << "dp[1] = 1（初始值：只能迈 1 级，1 种走法）" << endl;
    cout << "dp[2] = 2（初始值：1+1 或直接迈 2 级，2 种走法）" << endl;
    for (int i = 3; i <= n; i++) {
        dp[i] = dp[i - 1] + dp[i - 2];
        cout << "dp[" << i << "] = dp[" << (i - 1) << "] + dp[" << (i - 2) << "] = " << dp[i - 1] << " + " << dp[i - 2] << " = " << dp[i] << endl;
    }
    return dp[n];
}

int main() {
    int ans = climbStairs(5);
    cout << "爬到第 5 阶共有 " << ans << " 种走法" << endl;
    return 0;
}
\`\`\`

三个版本输出一致：

\`\`\`
dp[1] = 1（初始值：只能迈 1 级，1 种走法）
dp[2] = 2（初始值：1+1 或直接迈 2 级，2 种走法）
dp[3] = dp[2] + dp[1] = 2 + 1 = 3
dp[4] = dp[3] + dp[2] = 3 + 2 = 5
dp[5] = dp[4] + dp[3] = 5 + 3 = 8
爬到第 5 阶共有 8 种走法
\`\`\`

对着输出看：每一行用到的两个加数，都是**前面某一行已经算好的结果**——这就是「查表代替重算」的实锤。

### 直觉想法错在哪

既然能拆成小问题，最直觉的写法是直接递归：f(n) = f(n - 1) + f(n - 2)，三行搞定。问题是它慢得离谱。

别觉得夸张，把 n = 5 的递归树手动展开数一数：

- f(5) 需要算 f(4) 和 f(3)
- f(4) 又需要算 f(3) 和 f(2)
- f(3) 又需要算 f(2) 和 f(1)

光 n = 5，f(3) 就被算了 2 次、f(2) 被算了 3 次；n 稍大一点，重复就滚雪球成指数爆炸：总计算次数约 2ⁿ，n = 45 就是三百多亿次，要等好几分钟。

关键观察：这道题一共只有 n 个不同的小问题（f(1) 到 f(n)），朴素递归却算了约 2ⁿ 次，**多出来的全是重复劳动**。DP 给每个小问题发一格座位、只算一次，总成本降到 n 次加法——从等几分钟变成一眨眼。所以背模板不如想明白：**DP 不是一种新算法，是给递归装上了记忆**。

### 常见错误

- **初始值随手填、不验证**：dp[1]、dp[2] 填错，整张表后面全错。容易犯是因为注意力全在转移方程上，觉得边界「给个 1 就行」。对策：写完用 n = 1、2 手算验证。
- **遍历顺序错，读到空格**：自己设计填表顺序时，让某格用到了还没填的格子（读到默认的 0）。容易犯是因为只想到「谁用谁」，没想到「谁先算」。记住：被依赖的格子必须先填。
- **下标越界**：公式 dp[i] = dp[i - 1] + dp[i - 2] 只在 i ≥ 3 时成立，从 i = 0 开始循环会访问 dp[-2]——Python 还会静默地从数组末尾取值，错得无声无息。
- **以为答案永远在最后一格**：不少题（如 53 最大子数组和）的答案是整张表里的最大值。爬楼梯的答案刚好在 dp[n]，容易形成错误肌肉记忆；每道题先问一句「答案到底在哪一格（或哪几格）」。
- **一上来就写滚动数组优化**：两个变量滚来滚去，下标关系一乱全错。先写数组版跑通，再去压空间。

### 什么时候用

- 题目问**最多 / 最少 / 方案总数 / 最长 / 能否凑出**——只要数量或最优值，不要具体方案（要列方案去看《回溯算法》）
- 大问题能拆成同款小问题，而且小问题会**重复出现**（子问题互不重复的是分治，比如归并排序，存下来也没人查）
- 数据范围在暗示 O(n) 或 O(n²)：比如 n 到了 10⁵ 级别，O(2ⁿ) 暴力肯定超时，就该停下来找递推关系

三种问法在题库里的代表：**方案总数**（62 不同路径）、**最大 / 最小值**（53 最大子数组和、198 打家劫舍、322 零钱兑换）、**能否凑出**（416 分割等和子集）。

DP 和贪心的分界也在这里：贪心是「每步拿当前最优、不回头」，DP 是「把所有小问题的答案都存着慢慢选」。拿不准该用哪个时，先写 DP 更稳。

做题清单（建议抄在纸上贴屏幕边）：状态定义 → 转移方程 → 初始值 → 遍历顺序 → 写代码 → 拿小样例把整张表手算一遍。前四步想不清楚时**不要动手写代码**。

### 练习路径

按这个顺序刷，每题的预期收获写在后面：

- **70 爬楼梯**：本文例子，把四要素完整走一遍
- **198 打家劫舍**：转移里第一次出现「选不选」——dp[i] = max(dp[i - 1], dp[i - 2] + nums[i])
- **53 最大子数组和**：学会「答案不一定在最后一格」，填表时顺手记录历史最大值
- **62 不同路径**：从一维升到二维格子 dp[i][j]，第一行第一列全是初始值
- **322 零钱兑换**：完全背包入门，学会「凑不出的格子」用无穷大标记
- **300 最长递增子序列**：状态定义变成「以 i 结尾」，转移时要回头看所有前面的格子
- **1143 最长公共子序列**：双序列 DP 的模板题，两个字符串各留一段前缀做状态

每道题都先写记忆化搜索版本、再改成填表版本，两遍下来，四要素就刻进肌肉记忆了。
`,
};
