// 知识文章：动态规划
export default {
  "slug": "dynamic-programming",
  "title": "动态规划",
  "intro": "从打家劫舍推导状态，再理解初始化、计算顺序和空间压缩。",
  "tags": ["动态规划","记忆化搜索"],
  "relatedProblems": [
    {
      "id": 70,
      "stage": "core",
      "reason": "从最后一步推导依赖"
    },
    {
      "id": 198,
      "stage": "core",
      "reason": "枚举选或不选当前房间"
    },
    {
      "id": 62,
      "stage": "practice",
      "reason": "累加到达一格的方案数"
    },
    {
      "id": 322,
      "stage": "practice",
      "reason": "区分不可达状态与最少硬币数"
    },
    {
      "id": 416,
      "stage": "practice",
      "reason": "通过倒序避免重复使用当前数"
    },
    {
      "id": 1143,
      "stage": "practice",
      "reason": "定义两个前缀的公共子序列"
    },
    {
      "id": 72,
      "stage": "practice",
      "reason": "比较编辑操作的代价"
    }
  ],
  content: `
动态规划适合这样的问题：许多不同的选择过程会重复遇到同一个较小问题，而这个小问题的答案可以保存下来，供后续复用。

最难的一步通常是定义“同一个小问题”到底是什么。先把状态写成完整的一句话，再写公式，会比先猜一个 dp 数组更可靠。

### 从打家劫舍推出来

第 198 题给出每间房的钱数，不能同时选择相邻两间，求最多能拿多少。考虑最后一间房，只有两种情况：选它，或者不选它。

定义 dp[i]：只考虑前 i 间房，能拿到的最大金额。于是数组下标 i−1 对应这前 i 间里的最后一间。

不选最后一间，答案是 dp[i−1]。选最后一间，就不能选前一间，答案是 dp[i−2]+nums[i−1]。取两者较大值。

初始 dp[0]=0；若至少有一间房，dp[1]=nums[0]。这道题的钱数非负，因此这样初始化合理。

以 [2,7,9,3,1] 为例：

| 考虑前几间 | 不选当前房 | 选当前房 | 最大金额 |
| --- | --- | --- | --- |
| 1 | 0 | 2 | 2 |
| 2 | 2 | 7 | 7 |
| 3 | 7 | 2+9 | 11 |
| 4 | 11 | 7+3 | 11 |
| 5 | 11 | 11+1 | 12 |

最终答案是 12，选的是金额为 2、9、1 的房间。

### 为什么可以只保留最优答案

当我们准备选第 i 间房时，之前各个具体选择过程不再重要；只要知道前 i−2 间的最大金额即可，因为与当前房的相邻限制已经由“跳过前一间”处理了。

如果后续是否合法还取决于某个未记录的信息，例如剩余容量、上一件物品种类，那就必须把这些信息加进状态。不能把未来仍然需要区分的情况合并到同一个 dp[i]。

\`\`\`run-py#dynamic-programming-v2-demo
def rob(nums):
    prev2, prev1 = 0, 0
    for money in nums:
        current = max(prev1, prev2 + money)
        prev2, prev1 = prev1, current
    return prev1

print(rob([2, 7, 9, 3, 1]))
\`\`\`

\`\`\`run-js#dynamic-programming-v2-demo
function rob(nums) {
  let prev2 = 0, prev1 = 0;
  for (const money of nums) {
    const current = Math.max(prev1, prev2 + money);
    prev2 = prev1;
    prev1 = current;
  }
  return prev1;
}
console.log(rob([2, 7, 9, 3, 1]));
\`\`\`

\`\`\`run-cpp#dynamic-programming-v2-demo
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
int rob(const vector<int>& nums) {
    int prev2 = 0, prev1 = 0;
    for (int money : nums) {
        int current = max(prev1, prev2 + money);
        prev2 = prev1; prev1 = current;
    }
    return prev1;
}
int main() { cout << rob({2, 7, 9, 3, 1}) << '\\n'; }
\`\`\`

### 写状态转移时检查四件事

先明确状态代表的范围与答案类型：是最大值、最小值、方案数还是能否做到。接着枚举最后一步有哪些选择，说明这些选择覆盖了所有情况。然后处理不依赖其他状态的初始值。最后安排计算顺序，使被依赖的状态先算好。

对打家劫舍来说，只依赖前两个状态，所以示例用两个变量，时间 O(n)，额外空间 O(1)。刚学习时先写完整 dp 数组更便于检查，确认正确后再压缩空间。

### 最小值、方案数与可行性不能混用初始值

第 322 题零钱兑换求最少硬币数。定义 dp[a] 为凑出金额 a 的最少数量，dp[0]=0；其他暂时凑不出的金额应当是无穷大。若全初始化为 0，就会错误地认为所有金额都不花硬币。

对每个金额 a，尝试最后使用哪一种硬币 c，比较 dp[a−c]+1。金额从小到大计算；时间 O(硬币种数×金额)，空间 O(金额)。这依赖题目的硬币面额为正。

第 62 题不同路径统计方案数，到某格子的路径来自上方或左方，所以把两边数量相加。边界第一行、第一列在无障碍条件下各只有一条路径。

第 416 题分割等和子集只问能否做到。总和必须是偶数，再判断能否选一些数凑出一半。使用布尔值就足够，不必保存具体方案数量。

### 背包为什么有时从大往小遍历

第 416 题每个数只能用一次。把二维“考虑前几项、凑出多少和”压成一维后，处理当前数 x 时，要从目标和向下更新 dp[s] |= dp[s−x]。

逆序让 dp[s−x] 仍然是使用当前数之前的结果。若正序更新，刚写好的状态可能立刻被再次读取，等价于允许当前数使用多次。

第 322 题硬币可以重复用，依赖关系又不同。先看题意和状态定义，再决定顺序，别把“背包倒着遍历”当成所有动态规划的规则。

### 两个字符串的状态

第 1143 题最长公共子序列可以定义 dp[i][j] 为两个字符串前 i、前 j 个字符的答案。

末尾字符相等时，可以在较短两个前缀的答案上加一；不相等时，至少有一个末尾字符不能同时参加匹配，取去掉其中一侧后的较大答案。子序列允许不连续，和子串不同。

第 72 题编辑距离的状态范围类似，但值变成最少操作数，转移比较插入、删除、替换。初始 dp[i][0]=i、dp[0][j]=j，因为要处理从非空串到空串的情况。

### 记忆化搜索也是一种写法

可以从目标状态递归到所需的小状态，把每个结果缓存；也可以按依赖顺序从小到大填表。两者若计算相同状态和转移，工作量通常相近，但递归还需要考虑调用栈和语言的深度限制。

练习顺序建议是第 70 题、第 198 题、第 62 题、第 322 题、第 416 题、第 1143 题、第 72 题。写完后遮住代码，解释 dp[i] 或 dp[i][j] 的确切含义。如果一句话里出现“差不多表示”，先修改定义，再改公式。
`,
};
