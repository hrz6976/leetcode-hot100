// 知识文章：堆与贪心
// 数据格式约定（所有知识文章遵循）：
// - slug: 英文短横线命名，同时是文件名与路由
// - tags: 与题库中题目的 tags 匹配，用于自动关联题目
// - content: Markdown 子集（### 标题、- 列表、``` 代码块、`行内代码`）
export default {
  slug: 'heap-greedy',
  title: '堆与贪心',
  intro: '用堆动态维护「最值」，用贪心每步取局部最优：Top K 与中位数的标配',
  tags: ['堆（优先队列）', '贪心', '排序', '设计'],
  relatedProblems: [55, 215, 295, 347, 763],

  content: `
> 提示：读这篇文章前，最好已经具备这些基础：
> 1. 会排序，知道 O(n log n) 这类复杂度记号（《时间复杂度入门》讲过）；
> 2. 听过「树是父节点连子节点」的结构（《二叉树与递归》讲过）——堆的内部是一棵树，但本文不要求你会写树。

医院急诊不按挂号先后叫号，而是按病情严重度：每来一位病人就插入等候区，医生永远先叫当前最重的那位。这个「自动按优先级排队、每次交出最紧急的人」的装置，就是**堆**。贪心则是另一种玩家：超市限时抢购，每走到一个货架都拿眼前最划算的商品，赌走到收银台时总价最优——**每步只选局部最优，拿了就不回头**。

### 是什么

先说**堆**。大白话：一个袋子，你往里扔数字，它自己把数字整理好；但你看不到里面的排列，每次只能看、只能拿走「最顶上」的那个（最小值，或者最大值，取决于你选哪种堆）。扔进去、拿走都只要 O(log n)，看一眼最顶上只要 O(1)。

术语对照：

- **小顶堆**：堆顶永远是当前最小值；**大顶堆**：堆顶永远是当前最大值
- **堆顶**：唯一能直接访问的位置，相当于「急诊下一个被叫的人」
- **优先队列**：堆的另一个名字——不是先到先得，而是优先级高的先出
- **入堆 / 出堆**：塞进一个新数 / 拿走堆顶，每次操作后堆自动重新整理好，代价 O(log n)

划重点：堆只保证「堆顶是最值」，内部其余位置**不按大小排列**。它就是为「反复取最值」这一件事造的专车，别拿它当有序数组用。

> 注意：堆的内部其实是一棵「尽量填平」的二叉树（小顶堆里每个父节点都不大于它的子节点），但工程上直接用数组存放，不需要你手写树结构。每次入堆 / 出堆，堆只沿一条竖直路径调整，树的高度是 log n——这就是「每次操作 O(log n)」的来源。

再说**贪心**。大白话：每走一步，都在当前可选里拿「看着最好」的那个，拿完不回头、不反悔。代码短、跑得快，代价是**不保证对所有题都对**——策略选错了，样例还能蒙混过关，是最危险的算法类型。

这篇文章的两半正好是一对对照：堆解决「随时要最值」，贪心解决「敢不敢每步只选最优」——一个靠的是数据结构，一个靠的是胆子加证明。

### 一个最小例子

题目（215 数组中的第 K 个最大元素）：在 \`[3, 2, 1, 5, 6, 4]\` 里找出**第 2 大**的数。

思路：随身带一个**最多装 2 个数的小顶堆**，边读边维护——堆里始终装着「目前见过的最大的 2 个数」，堆顶就是这 2 个里较小的那个。

- 堆没满：新数直接进堆
- 堆满了：新数比堆顶大，就把堆顶踢出去、新数进堆（比堆顶还小的没资格留在前 2 名）；否则新数直接扔掉

全部读完后，堆顶 = 前 2 名里垫底的 = 全局第 2 大。逐步推演如下（堆的写法：堆顶永远在最左边）：

| 步骤 | 读到的数 | 本步判断 | 本步动作 | 操作后的堆（堆顶在最左） |
| --- | --- | --- | --- | --- |
| 1 | 3 | 堆没满（容量 2） | 3 直接进堆 | [3] |
| 2 | 2 | 堆没满（容量 2） | 2 直接进堆，最小的 2 坐上堆顶 | [2, 3] |
| 3 | 1 | 1 不大于堆顶 2 | 扔掉 1，堆不变 | [2, 3] |
| 4 | 5 | 5 比堆顶 2 大 | 弹出 2，放入 5 | [3, 5] |
| 5 | 6 | 6 比堆顶 3 大 | 弹出 3，放入 6 | [5, 6] |
| 6 | 4 | 4 不大于堆顶 5 | 扔掉 4，堆不变 | [5, 6] |
| 答案 | — | 全部读完 | 堆顶就是第 2 大 | 答案是 **5** |

为什么是对的：堆里每次淘汰的都是「当前前 2 名里最弱的」，能一直活到最后的，必定是全体数里的前 2 名；堆顶是前 2 名里较小的，正是第 2 大。把容量从 2 换成 k，同一个套路就能找第 k 大。

验算一下：[3, 2, 1, 5, 6, 4] 从大到小排是 [6, 5, 4, 3, 2, 1]，第 2 大确实是 5。堆给出了同样的答案，却不需要把整个数组排序。有人担心弹进弹出堆里会乱——记住堆的承诺只有一条：堆顶永远是堆里最小的，其余位置长什么样不用操心。

堆讲完了，再顺便看一眼贪心干活的样子（55 跳跃游戏）：数组 [2, 3, 1, 1, 4] 表示每个位置最远能跳几步，问能否从下标 0 跳到终点。贪心只维护一个变量 farthest =「目前最远能到达的下标」，边走边更新：

| 步骤 | 下标 i | nums[i]（能跳几步） | 本步判断 | farthest 更新 |
| --- | --- | --- | --- | --- |
| 起点 | — | — | 还没出发，只能站在下标 0 | 0 |
| 1 | 0 | 2 | i = 0 没超过 farthest，能走 | max(0, 0 + 2) = 2 |
| 2 | 1 | 3 | i = 1 没超过 farthest，能走 | max(2, 1 + 3) = 4 |
| 3 | — | — | farthest = 4 已是最后一个下标 | 判定：能到达 |

一旦发现 i > farthest，说明这个位置本身就到不了，直接判失败。全程没有枚举任何一种跳法——这就是贪心「每步只记最优」的模样。

### 模板代码

Python 版本（Python 自带堆库 heapq，是小顶堆）：

\`\`\`
# Python：215 题，找第 k 大
import heapq                        # Python 自带的堆工具，是小顶堆

def findKthLargest(nums, k):
    heap = []                       # 小顶堆：heap[0] 永远是堆顶（堆里的最小值）
    for x in nums:                  # 挨个读数
        if len(heap) < k:           # 堆没满（容量 k）
            heapq.heappush(heap, x) # 直接进堆，自动整理好
        elif x > heap[0]:           # 新数比堆顶大 → 堆顶没资格留在前 k 名
            heapq.heapreplace(heap, x)  # 弹出堆顶、放进新数，一步完成
        # else：新数不够格，什么都不用做
    return heap[0]                  # 堆顶 = 前 k 名里最小的 = 第 k 大
\`\`\`

JavaScript 版本（JS 没有内置堆，只找一次「第 k 大」时排序最务实）：

\`\`\`
// JavaScript：215 题，找第 k 大
function findKthLargest(nums, k) {
  nums.sort((a, b) => b - a);       // 从大到小排序；必须传比较函数，否则会按字符串排错
  return nums[k - 1];               // 排序后的第 k 个元素就是第 k 大
}
\`\`\`

C++ 版本（标准库 priority_queue 默认是大顶堆，这里指定成小顶堆）：

\`\`\`
// C++：215 题，找第 k 大
#include <queue>
#include <vector>
#include <functional>
using namespace std;

int findKthLargest(vector<int>& nums, int k) {
  // 小顶堆：heap.top() 永远是堆顶（堆里的最小值）
  priority_queue<int, vector<int>, greater<int>> heap;
  for (int x : nums) {              // 挨个读数
    if ((int)heap.size() < k) {     // 堆没满（容量 k）
      heap.push(x);                 // 直接进堆，自动整理好
    } else if (x > heap.top()) {    // 新数比堆顶大 → 堆顶没资格留在前 k 名
      heap.pop();                   // 弹出堆顶
      heap.push(x);                 // 放进新数
    }
    // else：新数不够格，什么都不用做
  }
  return heap.top();                // 堆顶 = 前 k 名里最小的 = 第 k 大
}
\`\`\`

JS 的排序版是 O(n log n)，刷题通常够用；只有当数据持续流入、或明确要求 O(n log k) 时，才需要照 Python 版的思路手写一个小顶堆类——「容量 k、顶最小、大了就换」这三步完全不变。

### 亲手跑一跑

在 [3, 2, 1, 5, 6, 4] 上找第 2 大，把堆里数字的每一次进出都打印出来。JS 版用一个时刻保持有序的小数组来模拟小顶堆（真正的堆里「整理」是 O(log n)，这里模拟只为看清过程）；Python 版用真堆 heapq，C++ 版用标准库 priority_queue。三个版本的输出完全一致。

如果你只想看真正的堆 API，直接读 Python 版或 C++ 版；JS 版的价值是看清「容量固定为 k」这个不变量是怎么手动维持的。

JavaScript：

\`\`\`run-js#top-k-heap
const nums = [3, 2, 1, 5, 6, 4];    // 输入数组
const k = 2;                         // 找第 2 大
const heap = [];                     // 模拟小顶堆：时刻保持从小到大，heap[0] 即堆顶
for (const x of nums) {
  if (heap.length < k) {             // 堆没满：直接进堆
    heap.push(x);
    heap.sort((a, b) => a - b);
    console.log('读到 ' + x + '：堆没满，直接进堆 → 堆 = [' + heap.join(', ') + ']，堆顶 = ' + heap[0]);
  } else if (x > heap[0]) {          // 新数比堆顶大：换进
    const out = heap.shift();        // 弹出堆顶
    heap.push(x);
    heap.sort((a, b) => a - b);
    console.log('读到 ' + x + '：比堆顶 ' + out + ' 大，弹 ' + out + ' 进 ' + x + ' → 堆 = [' + heap.join(', ') + ']，堆顶 = ' + heap[0]);
  } else {                           // 新数不够格：丢掉
    console.log('读到 ' + x + '：不比堆顶 ' + heap[0] + ' 大，丢掉');
  }
}
console.log('第 ' + k + ' 大的数是：' + heap[0]);
\`\`\`

Python：

\`\`\`run-py#top-k-heap
import heapq                         # Python 自带的小顶堆
nums = [3, 2, 1, 5, 6, 4]            # 输入数组
k = 2                                # 找第 2 大
heap = []                            # 小顶堆：heap[0] 永远是堆顶（最小值）
for x in nums:
    if len(heap) < k:                # 堆没满：直接进堆
        heapq.heappush(heap, x)
        print('读到 ' + str(x) + '：堆没满，直接进堆 → 堆 = [' + ', '.join(map(str, heap)) + ']，堆顶 = ' + str(heap[0]))
    elif x > heap[0]:                # 新数比堆顶大：换进
        out = heap[0]
        heapq.heapreplace(heap, x)   # 弹出堆顶、放进新数
        print('读到 ' + str(x) + '：比堆顶 ' + str(out) + ' 大，弹 ' + str(out) + ' 进 ' + str(x) + ' → 堆 = [' + ', '.join(map(str, heap)) + ']，堆顶 = ' + str(heap[0]))
    else:                            # 新数不够格：丢掉
        print('读到 ' + str(x) + '：不比堆顶 ' + str(heap[0]) + ' 大，丢掉')
print('第 ' + str(k) + ' 大的数是：' + str(heap[0]))
\`\`\`

\`\`\`run-cpp#top-k-heap
#include <functional>
#include <iostream>
#include <queue>
#include <string>
#include <vector>
using namespace std;

// 把堆内容拷贝一份、从小到大弹出，仅用于打印（小顶堆依次弹出即升序）
string show(priority_queue<int, vector<int>, greater<int>> heap) {
  string s = "[";
  bool first = true;
  while (!heap.empty()) {
    if (!first) s += ", ";
    s += to_string(heap.top());
    heap.pop();
    first = false;
  }
  return s + "]";
}

int main() {
  vector<int> nums = {3, 2, 1, 5, 6, 4};  // 输入数组
  int k = 2;                              // 找第 2 大
  // 小顶堆：heap.top() 永远是堆顶（最小值）
  priority_queue<int, vector<int>, greater<int>> heap;
  for (int x : nums) {
    if ((int)heap.size() < k) {           // 堆没满：直接进堆
      heap.push(x);
      cout << "读到 " << x << "：堆没满，直接进堆 → 堆 = " << show(heap) << "，堆顶 = " << heap.top() << "\\n";
    } else if (x > heap.top()) {          // 新数比堆顶大：换进
      int out = heap.top();
      heap.pop();                         // 弹出堆顶
      heap.push(x);                       // 放进新数
      cout << "读到 " << x << "：比堆顶 " << out << " 大，弹 " << out << " 进 " << x << " → 堆 = " << show(heap) << "，堆顶 = " << heap.top() << "\\n";
    } else {                              // 新数不够格：丢掉
      cout << "读到 " << x << "：不比堆顶 " << heap.top() << " 大，丢掉" << "\\n";
    }
  }
  cout << "第 " << k << " 大的数是：" << heap.top() << "\\n";
  return 0;
}
\`\`\`

预期输出：

\`\`\`
读到 3：堆没满，直接进堆 → 堆 = [3]，堆顶 = 3
读到 2：堆没满，直接进堆 → 堆 = [2, 3]，堆顶 = 2
读到 1：不比堆顶 2 大，丢掉
读到 5：比堆顶 2 大，弹 2 进 5 → 堆 = [3, 5]，堆顶 = 3
读到 6：比堆顶 3 大，弹 3 进 6 → 堆 = [5, 6]，堆顶 = 5
读到 4：不比堆顶 5 大，丢掉
第 2 大的数是：5
\`\`\`

逐行对照上一节的表格：第 3 行和第 6 行「丢掉」的就是两个没资格进前 2 名的数，第 4、5 行是堆顶的两次换人。还可以把 k 改成 3 再跑一遍，答案应该变成 3——体会「容量 k ↔ 第 k 大」的对应关系。

### 直觉想法错在哪

**对堆来说**，最直觉的做法是「每次需要最值，就把数组整个排一遍序」——能算对，但每来一个新数就排一次，一万个数就是上亿次比较。也有人想「扫一遍找最大、删掉，再扫，做 k 次」：O(n·k)，k 大时比排序还慢。更要命的是**流式数据**：像「随时报告当前中位数」（295 题），数据边来边问，永远等不到「全部到齐再排序」的那一天。堆把维护最值的成本摊到每次 O(log n)，是怎么加新数都不慌的在线方案。

顺带说句公道话：215 这种只问一次「第 K 大」的题，直接排序 O(n log n) 是能过的，算不上「错」。堆真正的主场是「反复问」「边来边问」——数据有几百万个而 k 很小时，O(n log k) 的堆版也把排序版甩开很远。

**对贪心来说**，最直觉的误区是「每步都拿最好的，最后肯定也是最好的」。经典反例：面额 [1, 3, 4] 凑 6 元，贪心先拿 4，剩 2 只能用 1 + 1，共 3 枚；最优解其实是 3 + 3 两枚。**贪心策略必须先验证再使用**，靠「感觉合理」不行。

### 常见错误

- **JS 里 sort() 不传比较函数**：默认按字符串字典序排，[10, 9, 80] 会被排成 [10, 80, 9]。容易犯是因为个位数时碰巧正确，一上两位数立刻翻车。
- **Python 里想要大顶堆，就 push 原值再取列表末尾**：heapq 只有小顶堆，而且堆不保证末尾是最大值。正确姿势是存负数变身大顶堆（push \`-x\`，弹出后再取负还原）。容易犯是因为把堆想成了「排好序的数组」。
- **用 heap[k - 1] 直接取第 k 大**：堆里只有堆顶保证是最值，其余位置没有顺序。要么像本文一样控制堆的大小，要么连弹 k 次。容易犯的原因同上——堆不是有序数组。
- **贪心凭感觉上手**：策略「听起来对」就写，结果样例过了、隐藏用例全挂。先花两分钟构造反例，构造不出再想证明（交换论证），想不清就老实写 DP 或暴力对拍。
- **堆满了还继续 push 不弹出**：容量 k 是这个技巧的根基，忘了弹出，Top K 变成 Top 全部，最后堆顶变成全局最小值。

### 什么时候用

堆的信号：

- **Top K 类**：第 K 大（215）、前 K 个高频（347）——题目里出现「第 K / 前 K 个最……」
- **流式 / 在线数据**：数据一个个来，随时要问最值，如数据流中位数（295）、合并 K 个有序链表
- **带优先级的调度**：任务调度、定时器里「最先到期的先执行」，底层也是它
- 一句话判断：**需要反复、动态地取「当前最大 / 最小」**，就用堆

贪心的信号：

- 每一步的选择**不受之前决定的影响**（无后效性），且有直观的选择标准（按结束时间排序选、维护最远可达位置等）
- 求「最多 / 最少 / 能否」，且数据范围大到 O(n²) 的 DP 会超时
- 策略想得出来、反例构造不出，再尝试证明；证明不了就换 DP 或回溯

### 练习路径

堆线（由浅入深）：

- **215 数组中的第 K 个最大元素**：先排序 AC，再照本文思路实现一遍「容量 k 的小顶堆」
- **347 前 K 个高频元素**：学会「哈希表统计频率 + 堆取 Top K」的组合拳
- **295 数据流中位数**：两个堆各管一半数据，堆思想的大考，也是「流式数据」的招牌题

贪心线（由浅入深）：

- **55 跳跃游戏**：维护「最远可达下标」，一个变量贪到底，体会局部最优如何滚成全局
- **763 划分字母区间**：先预处理每个字母最后出现的位置——贪心要学会「先侦察再动手」
- **45 跳跃游戏 II**：在 55 的基础上求最少步数，体会贪心策略的细微差别
`,
};
