// 知识文章：滑动窗口
// 数据格式约定（所有知识文章遵循）：
// - slug: 英文短横线命名，同时是文件名与路由
// - tags: 与题库中题目的 tags 匹配，用于自动关联题目
// - relatedProblems（可选）: 显式指定关联题目 id，优先于 tags 匹配
// - content: Markdown 子集（### 标题、- 列表、``` 代码块、`行内代码`）
export default {
  slug: 'sliding-window',
  title: '滑动窗口',
  intro: '用两个指针维护一个「窗口」，在 O(n) 时间内处理子串 / 子数组问题',
  tags: ['滑动窗口'],

  content: `
> 提示：读这篇之前，你只需要会两件事：用下标取字符（\`s[i]\` 是字符串第 i 个字符）、写最基础的 \`for\` / \`while\` 循环。如果你还没读过《双指针》，建议先读它——滑动窗口就是双指针里最常用的一种形态；对 O(n)、O(n²) 这类说法没概念的话，可以先翻《时间复杂度入门》。

坐公交车时，车窗外的景色一帧一帧往后退，但**你眼里永远只有窗框内的那一小段**。车往前开，窗框右边吞进新景色、左边吐出旧景色，你从不回头重看任何一段路。滑动窗口干的就是这件事：在数组 / 字符串上框出一个「窗口」，只关心窗内的元素，窗口一路向右滑，全程不回头。

### 是什么

先用大白话说：很多题目要你在一个字符串里找「**连续的一小段**」，比如「没有重复字符的最长连续段」。笨办法是把每一小段都拎出来检查一遍；滑动窗口的做法是——

- 用两个标记 \`left\` 和 \`right\` 夹住当前这一段（这就是窗口）
- \`right\` 一格一格往右挪，每挪一次就把新字符「吞」进窗口
- 一旦窗口里的内容违规了（比如出现了重复字符），就把 \`left\` 往右挪，从左边「吐」字符，吐到重新合规为止
- 两个标记**都只往右走、绝不回头**，所以每个字符最多被吞一次、被吐一次

术语对照：\`left\`、\`right\` 叫**指针**，这里它们只是两个存下标的普通变量；\`left\` 到 \`right\` 之间的连续区间叫**窗口**；\`left\` 右移叫**收缩窗口**，\`right\` 右移叫**扩张窗口**。

为什么它快？因为不回头。每个字符进出窗口各一次，n 个字符最多 2n 次操作，**总时间 O(n)**。而「把每一小段都拎出来看」有 n² 级别的小段，每段还要逐个字符检查，是 O(n²) 甚至 O(n³)。

回头补一句和公交车类比的对应关系：\`right\` 就是窗框的右边框，负责往前看新风景；\`left\` 是左边框，负责把看过的旧风景放走。你（程序）从头到尾只做两件事——右边吞、左边吐。

> 重点：先记住一句话——right 负责探索新世界，left 负责收拾旧摊子。两个指针分工明确、互不回头，这是所有滑窗题共同的心智模型。

题库里的滑窗题其实就三种形态，骨架都一样，只是换零件：

- **可变窗口求最长**：窗口一违规就收缩，收缩完成后更新答案。本文的例子（第 3 题）就是这种
- **可变窗口求最短**：窗口一旦满足条件就不停收缩，**每缩一步**都尝试刷新答案（第 76 题）
- **固定长度窗口**：窗口大小恒等于某个 k，右边进一个、左边就必须出一个（第 438 题）

### 一个最小例子

拿经典题「无重复字符的最长子串」练手：\`s = "abcabcbb"\`，要找出**没有重复字符的最长连续段**的长度（答案是 \`"abc"\`，长度 3）。

规则就一条：窗口里不许出现重复字符。\`right\` 每吞一个新字符，先看它是不是已经在窗口里——在，就不停地从左边吐，直到它不在了，再吞进来。

拿一张表逐轮跟踪（\`win\` 是一个集合，记着当前窗口里有哪些字符）：

| 轮次 | right 读到的字符 | 收缩过程（从左边踢出谁， left 怎么动） | 加入后窗口 [left, right] | 窗口内容 | 本轮长度 | 历史最长 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | s[0]='a' | 窗口是空的，直接进 | [0,0] | "a" | 1 | 1 |
| 2 | s[1]='b' | 窗口里没有 'b'，直接进 | [0,1] | "ab" | 2 | 2 |
| 3 | s[2]='c' | 窗口里没有 'c'，直接进 | [0,2] | "abc" | 3 | 3 |
| 4 | s[3]='a' | 'a' 已在！踢 s[0]='a'，left 0→1，再查 'a' 已不在 | [1,3] | "bca" | 3 | 3 |
| 5 | s[4]='b' | 'b' 已在！踢 s[1]='b'，left 1→2 | [2,4] | "cab" | 3 | 3 |
| 6 | s[5]='c' | 'c' 已在！踢 s[2]='c'，left 2→3 | [3,5] | "abc" | 3 | 3 |
| 7 | s[6]='b' | 'b' 已在！踢 s[3]='a'（left→4）还有 'b'，再踢 s[4]='b'（left→5） | [5,6] | "cb" | 2 | 3 |
| 8 | s[7]='b' | 'b' 已在！踢 s[5]='c'（left→6）还有 'b'，再踢 s[6]='b'（left→7） | [7,7] | "b" | 1 | 3 |

八轮走完，历史最长是 **3**，对应窗口 "abc"（第 3 轮和第 6 轮都达到过）。

盯住表格里两件事，就看懂了滑窗的灵魂：

- \`right\` 从 0 走到 7，一步没回头；\`left\` 从 0 挪到 7，也一步没回头
- 第 7、8 轮各踢了两次才合规——所以收缩必须是 **while 循环**，不是 if

### 模板代码

上面这张表翻译成代码，几乎一行对应一步：

\`\`\`
// JavaScript：无重复字符的最长子串（LeetCode 3）
function lengthOfLongestSubstring(s) {
  const win = new Set();            // 窗口集合：记着窗口里现在有哪些字符
  let left = 0;                     // 窗口左边界（下标）
  let maxLen = 0;                   // 历史最长窗口的长度
  for (let right = 0; right < s.length; right++) {  // 右边界一格一格右移
    while (win.has(s[right])) {     // 新字符已在窗口里 → 必须收缩
      win.delete(s[left]);          //   把最左边的字符踢出集合
      left++;                       //   左边界右移一格
    }                               // 循环结束时，窗口里一定没有 s[right] 了
    win.add(s[right]);              // 放心把新字符吞进窗口
    maxLen = Math.max(maxLen, right - left + 1);  // 窗口长度 = 右 - 左 + 1，刷新纪录
  }
  return maxLen;                    // 走完全程，纪录就是答案
}
\`\`\`

\`\`\`
# Python：无重复字符的最长子串（LeetCode 3）
def lengthOfLongestSubstring(s):
    win = set()                     # 窗口集合：记着窗口里现在有哪些字符
    left = 0                        # 窗口左边界（下标）
    max_len = 0                     # 历史最长窗口的长度
    for right in range(len(s)):     # 右边界一格一格右移
        while s[right] in win:      # 新字符已在窗口里 → 必须收缩
            win.remove(s[left])     #   把最左边的字符踢出集合
            left += 1               #   左边界右移一格
        win.add(s[right])           # 现在窗口里没有它了，放心吞进来
        max_len = max(max_len, right - left + 1)  # 窗口长度 = 右 - 左 + 1，刷新纪录
    return max_len                  # 走完全程，纪录就是答案
\`\`\`

\`\`\`
// C++：无重复字符的最长子串（LeetCode 3）
int lengthOfLongestSubstring(string s) {
  unordered_set<char> win;          // 窗口集合：记着窗口里现在有哪些字符
  int left = 0;                     // 窗口左边界（下标）
  int maxLen = 0;                   // 历史最长窗口的长度
  for (int right = 0; right < (int)s.size(); right++) {  // 右边界一格一格右移
    while (win.count(s[right])) {   // 新字符已在窗口里 → 必须收缩
      win.erase(s[left]);           //   把最左边的字符踢出集合
      left++;                       //   左边界右移一格
    }                               // 循环结束时，窗口里一定没有 s[right] 了
    win.insert(s[right]);           // 放心把新字符吞进窗口
    maxLen = max(maxLen, right - left + 1);  // 窗口长度 = 右 - 左 + 1，刷新纪录
  }
  return maxLen;                    // 走完全程，纪录就是答案
}
\`\`\`

其他滑窗题都是在这个骨架上换三个零件：①「窗口里记什么」（字符集合、字符计数、元素和）；②「什么算违规」（有重复、和超过 k、还没凑齐所有字符）；③「什么时候更新答案」（最长类在收缩**之后**更新，最短类在收缩**过程中**每步尝试更新）。

背模板别背整段代码，背这四句口诀就能现场还原：

1. right 右移一格，准备吞入新元素
2. 违规了？while 循环从左边吐，吐到合规为止
3. 把新元素真正吞进窗口（和 2 的顺序不能反！）
4. 按题型在该在的位置更新答案（最长类在这里，最短类在第 2 步的循环里）

### 亲手跑一跑

下面两段代码逻辑完全一样，跑的就是上面那张表。把窗口的每轮变化都打印了出来，你可以亲眼看着 \`left\` 被一格一格推着走：

\`\`\`run-js#window-walkthrough
const s = 'abcabcbb';
const win = new Set();          // 当前窗口里的字符
let left = 0, maxLen = 0;
for (let right = 0; right < s.length; right++) {
  const c = s[right];
  while (win.has(c)) {          // 新字符已在窗口里，从左边踢，直到不重复
    console.log('  窗口里已有 ' + c + '，踢出最左边的 ' + s[left] + '，left ' + left + ' -> ' + (left + 1));
    win.delete(s[left]);
    left++;
  }
  win.add(c);                   // 吞入新字符
  maxLen = Math.max(maxLen, right - left + 1);
  console.log('right=' + right + ' 吞入 ' + c + '，窗口 [' + left + ',' + right + '] = "' + s.slice(left, right + 1) + '"，历史最长 ' + maxLen);
}
console.log('答案：' + maxLen);
\`\`\`

\`\`\`run-py#window-walkthrough
s = 'abcabcbb'
win = set()                     # 当前窗口里的字符
left = 0
max_len = 0
for right in range(len(s)):
    c = s[right]
    while c in win:             # 新字符已在窗口里，从左边踢，直到不重复
        print(f'  窗口里已有 {c}，踢出最左边的 {s[left]}，left {left} -> {left + 1}')
        win.remove(s[left])
        left += 1
    win.add(c)                  # 吞入新字符
    max_len = max(max_len, right - left + 1)
    print(f'right={right} 吞入 {c}，窗口 [{left},{right}] = "{s[left:right + 1]}"，历史最长 {max_len}')
print('答案：' + str(max_len))
\`\`\`

\`\`\`run-cpp#window-walkthrough
#include <iostream>
#include <string>
#include <unordered_set>
#include <algorithm>
using namespace std;

int main() {
  string s = "abcabcbb";
  unordered_set<char> win;      // 当前窗口里的字符
  int left = 0, maxLen = 0;
  for (int right = 0; right < (int)s.size(); right++) {
    char c = s[right];
    while (win.count(c)) {      // 新字符已在窗口里，从左边踢，直到不重复
      cout << "  窗口里已有 " << c << "，踢出最左边的 " << s[left] << "，left " << left << " -> " << (left + 1) << "\\n";
      win.erase(s[left]);
      left++;
    }
    win.insert(c);              // 吞入新字符
    maxLen = max(maxLen, right - left + 1);
    cout << "right=" << right << " 吞入 " << c << "，窗口 [" << left << "," << right << "] = \\"" << s.substr(left, right - left + 1) << "\\"，历史最长 " << maxLen << "\\n";
  }
  cout << "答案：" << maxLen << "\\n";
  return 0;
}
\`\`\`

预期输出（三个版本完全一致）：

\`\`\`
right=0 吞入 a，窗口 [0,0] = "a"，历史最长 1
right=1 吞入 b，窗口 [0,1] = "ab"，历史最长 2
right=2 吞入 c，窗口 [0,2] = "abc"，历史最长 3
  窗口里已有 a，踢出最左边的 a，left 0 -> 1
right=3 吞入 a，窗口 [1,3] = "bca"，历史最长 3
  窗口里已有 b，踢出最左边的 b，left 1 -> 2
right=4 吞入 b，窗口 [2,4] = "cab"，历史最长 3
  窗口里已有 c，踢出最左边的 c，left 2 -> 3
right=5 吞入 c，窗口 [3,5] = "abc"，历史最长 3
  窗口里已有 b，踢出最左边的 a，left 3 -> 4
  窗口里已有 b，踢出最左边的 b，left 4 -> 5
right=6 吞入 b，窗口 [5,6] = "cb"，历史最长 3
  窗口里已有 b，踢出最左边的 c，left 5 -> 6
  窗口里已有 b，踢出最左边的 b，left 6 -> 7
right=7 吞入 b，窗口 [7,7] = "b"，历史最长 3
答案：3
\`\`\`

对着输出回看那张推演表，每一行都能对上——这就是滑窗在机器里真实的样子。

三个值得盯住的输出细节：

- \`right=\` 那一列从 0 单调涨到 7，\`left\` 也是只涨不跌——「不回头」在输出里看得一清二楚
- 每出现一行「踢出」，窗口左边界就右移一格；连续两行「踢出」，就是 while 循环一次弹了两个
- 「历史最长」只增不减，最终停在 3——它记录的是见过的最好成绩，不是当前窗口长度

### 直觉想法错在哪

多数人第一直觉是：「最长子串嘛，那就把每个起点、每个终点都试一遍，检查这一段有没有重复字符」。这个思路写在纸上很自然，但它有两个致命伤：

- 起点有 n 种、终点有 n 种，组合出约 n²/2 个区间；每检查一个区间又要扫一遍区间内的字符，O(n)。三层叠起来是 **O(n³)**
- 更心疼的是：大量工作是完全重复的。比如你已经确认 \`s[0..2]="abc"\` 无重复，试 \`s[0..3]\` 时又把 a、b、c 从头查了一遍——**查过的信息全扔了**

滑窗正是把这个浪费堵上的：\`left\` 不回头，意味着「前面的区间已经查过、结论还记得」。right=3 发现 'a' 重复时，我们不用再扫窗口，因为我们知道重复的就是窗口里最左边那个 'a'，直接踢掉左端即可。信息被攒了下来，于是从 O(n³) 掉到 O(n)。n = 10⁵ 时，一个是 10¹⁵ 次操作（跑到天荒地老），一个是 2×10⁵ 次（一瞬间）。

### 常见错误

- **把收缩写成 if 而不是 while**：重复字符不一定在窗口最左边。比如窗口 "abc" 吞入 'b'，要连踢 a、b 两次才合规，if 只踢一次就吞，窗口里就有两个 'b' 了
- **窗口长度算错**：长度是 \`right - left + 1\`，不是 \`right - left\`。下标 0 到 2 是 3 个字符，不是 2 个——总忘记 +1 是因为把「下标差」当成了「个数」
- **用数组记窗口再 \`includes\` / \`in\` 判断**：每次判断要把数组扫一遍，O(n)，整体退化成 O(n²)。判断「在不在窗口里」就该用哈希集合（JS 的 \`Set\`、Python 的 \`set\`），一次查好
- **更新答案的时机不对**：最长类要在收缩**完成后**再量窗口（此时才保证合规）；最短类（如最小覆盖子串）恰恰相反，要在收缩**过程中**每步都尝试更新。照抄模板不问位置，换个题型就错
- **加入新字符和收缩的顺序颠倒**：先 \`add\` 再判断，新字符永远「已在窗口里」，收缩逻辑直接乱套；永远是「先收缩到合规，再吞入」

### 什么时候用

看到这几个信号凑在一起，优先考虑滑动窗口：

- 求的是**连续**子串 / 子数组（题面关键词：子串、子数组、连续）
- 这一段要满足某种约束（无重复、和不超过 k、必须包含某些字符）
- 问最长 / 最短 / 有多少个

反过来，看到「**子序列**」两个字就要警惕：子序列不要求连续（可以跳着选），滑窗管不了，那通常是动态规划的地盘（见《动态规划入门》）。

典型题目：3（无重复字符的最长子串）、438（找到字符串中所有字母异位词，窗口长度固定）、76（最小覆盖子串，最短类）、239（滑动窗口最大值，需搭配单调队列，见《栈、队列与单调栈》）。

### 练习路径

- **3 无重复字符的最长子串**：就是本文的例子，目标是把模板默写到不假思索。写完把 \`s\` 换成 \`"bbbbb"\`、\`"pwwkew"\` 在纸上各推一遍，验证 while 收缩真的想通了
- **438 找到字符串中所有字母异位词**：学会把「窗口里记什么」从字符集合换成字符计数（每个字母出现几次），并处理固定长度窗口的同步进出
- **76 最小覆盖子串**：最短类滑窗，体会「答案在收缩过程中更新」和「收缩完成后更新」的位置差别。这题偏难，卡住时先把「什么时候算窗口合法」这一件事想明白再写码
`,
};
