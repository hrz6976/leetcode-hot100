// 知识文章：栈、队列与单调栈
// 数据格式约定（所有知识文章遵循）：
// - slug: 英文短横线命名，同时是文件名与路由
// - tags: 与题库中题目的 tags 匹配，用于自动关联题目
// - relatedProblems（可选）: 显式指定关联题目 id，优先于 tags 匹配
// - content: Markdown 子集（### 标题、- 列表、``` 代码块、`行内代码`）
export default {
  slug: 'stack-queue',
  title: '栈、队列与单调栈',
  intro: '栈、队列、单调栈与单调队列：括号匹配到滑动窗口最大值',
  tags: ['栈', '队列', '单调栈', '单调队列', '设计'],

  content: `
> 提示：读这篇只需要会数组和 \`for\` / \`while\` 循环，没看过前面的文章也能跟上。如果已经读过《数组与哈希表》，本文里「数组当栈用」的部分会更亲切；文末练习里的 239 会用到《滑动窗口》的思想，可以回头再补。

想想两个生活场景。一是食堂里**叠起来的盘子**：洗碗工把干净盘子一个个摞上去，打饭的人从顶上拿——最后放上去的盘子最先被拿走。二是**排队买票**：队尾进、队头出，先来的人先买到。这两个场景就是栈和队列的全部本质，剩下的只是给它们起名字。

### 是什么

先用大白话定义，再给术语对照。

**栈**：一摞盘子。放东西只能从顶上放，拿东西也只能从顶上拿。谁最后放上去，谁最先被拿走。

- 术语：放进栈顶叫**压栈**（push），从栈顶拿走叫**出栈**（pop），看一眼栈顶叫 peek
- 这个规则叫 **LIFO**（Last In First Out，后进先出）
- JS 直接拿数组当栈：\`push\` / \`pop\` 都是 O(1)——数组尾部增删不需要挪动任何其他元素（为什么，《数组与哈希表》讲过）；Python 用 \`list.append\` / \`list.pop\`，同理
- 看栈顶：JS 写 \`stack[stack.length - 1]\`，Python 写 \`stack[-1]\`；操作前先判断栈是不是空的

顺便想一个问题：为什么没人讨论「从栈底抽一个盘子」？因为那根本不是栈允许的操作——想两端都能进出，那是双端队列的地盘。**一个数据结构的名字，本质上就是它允许的那几个操作**。

**队列**：一队买票的人。进从队尾进，出从队头出。谁先来，谁先被服务。

- 术语：进队叫**入队**，出队叫**出队**，规则叫 **FIFO**（First In First Out，先进先出）
- Python 用 \`collections.deque\`：\`append\` 入队、\`popleft\` 出队，都是 O(1)
- JS 没有现成队列，用数组加一个头指针 \`head\`：入队 \`arr.push(x)\`，出队就是 \`head++\`。**千万别用 \`shift()\` 出队**——它每出队一次都要把后面所有元素前移一位，是 O(n)（为什么会这样，《数组与哈希表》里讲过数组连续存储的代价）
- 头指针写法的直觉：\`head\` 就是队伍前面贴的一张「已服务到第几位」的纸条，出队只是把纸条号码加一，队伍里的人一个都不用动

两种结构的默念口诀：栈是「最近的要最先处理」，队列是「先来的先服务」。

操作速查（全部 O(1)，背下来就够刷题用了）：

- 栈 JS：\`push\` 压栈、\`pop\` 出栈、\`stack[stack.length - 1]\` 看栈顶、\`stack.length === 0\` 判空
- 栈 Python：\`append\` 压栈、\`pop\` 出栈、\`stack[-1]\` 看栈顶、\`not stack\` 判空
- 队列 Python：\`deque\` 的 \`append\` 入队、\`popleft\` 出队
- 队列 JS：数组 \`push\` 入队、头指针 \`head++\` 出队（\`arr[head]\` 是队首）

两个直觉锚点帮你记牢：浏览器的**后退按钮**就是一个栈——最近访问的页面最先被退回去；奶茶店的**取餐叫号**就是一个队列——先下单的先被叫到。

拿后退按钮推 3 步找找感觉：你依次打开 A → B → C 三个页面，浏览器内部把它们压成栈 \`[A, B, C]\`（栈顶在右）。点一次后退，C 出栈、回到 B；再点一次，B 出栈、回到 A。**每次操作的永远是栈顶那一个**——这就是「最近的最先处理」。

从普通栈到**单调栈**只差一条规矩：入栈时，会破坏单调性的元素先被请出去。这条规矩一加，栈里每个元素就带上了额外信息——关于「它旁边第一个比它大 / 小的元素是谁」，答案会被安排得明明白白。下面用一道题把它讲透。

### 一个最小例子

光堆概念记不住，直接上单调栈的经典题 **739. 每日温度**：给一个温度数组 \`t = [30, 40, 35, 50]\`，对每一天回答「**再等几天才会有更高的温度**」，后面没有更热的就答 0。正确答案是 \`[1, 2, 1, 0]\`。

先把「单调栈」说人话：它是一摞**按规矩叠的盘子**，规矩是栈里躺着的温度从底到顶越来越低（递减）。栈里躺着的每一天，都是一个**还没等到答案的「等待者」**。新的一天进来时看一眼栈顶：

- 今天**不比**栈顶热 → 栈顶那天还没等到答案，今天也进栈一起等
- 今天**比**栈顶热 → 栈顶那天的答案就是今天！把它弹出去记答案；弹完再和新的栈顶比，直到没有再比今天更冷的为止，然后今天进栈

为什么弹完还要继续比？因为今天可能是「大救星」——栈里积压的等待者全在等一个更热的天，今天也许能一口气把好几个都结算掉。这正是表格里第 3 天要发生的事。

注意栈里存的是**下标**（第几天），不是温度——因为「等几天」要用下标减出来。逐轮跟踪：

| 轮到 | 温度 | 和栈顶的比较与弹出 | 记下的答案 | 弹完压入谁 | 栈内下标（底→顶） |
| --- | --- | --- | --- | --- | --- |
| 第 0 天 | 30 | 栈是空的，没得比，直接进 | | 0 | [0] |
| 第 1 天 | 40 | 40 > t[0]=30，把 0 弹掉（栈空了，停） | ans[0] = 1-0 = **1** | 1 | [1] |
| 第 2 天 | 35 | 35 < t[1]=40，不弹 | | 2 | [1, 2] |
| 第 3 天 | 50 | 50 > t[2]=35，弹 2；50 > t[1]=40，再弹 1；栈空，停 | ans[2] = 3-2 = **1**；ans[1] = 3-1 = **2** | 3 | [3] |

循环结束后，栈里还躺着第 3 天——它后面没有更热的天了，答案保持默认的 0。最终 ans = **[1, 2, 1, 0]**。

先排掉一个常见误解：有人觉得「和相邻的下一天比一下不就行了」。看 \`[30, 35, 33]\`：第 1 天（35 度）往后，紧挨着的第 2 天 33 并不比它热，答案要跳到更远的某一天才出现——**只盯相邻元素会漏掉远处的答案**，这正是要维护一整个「等待者名单」（栈）的原因。

顺便回答另一个很多人卡住的点：**为什么栈里存下标，不直接存温度？** 因为「等几天」是两个下标减出来的（3 - 1 = 2 天）。温度随时能通过 \`t[下标]\` 查到，下标却没法从温度反查回去——所以栈里永远存下标，需要温度时再回数组里取。

盯住表格想通的两个关键：

- 第 3 天一口气「结算」了第 1、2 天两天的答案——这就是单调栈快的来源：**更热的天一来，积压的问题批量解决**
- 栈里躺着的永远是「还在等答案」的下标，而且温度从底到顶递减，不然栈顶早就该被弹掉了
- 栈被弹空的时刻是分水岭：空栈说明当前这一天把前面所有等待者都结清了，它自己进栈重新开始当「最新的等待者」

### 模板代码

上面这张表翻译过来就是每日温度的模板，读注释时对照着表格的每一行。建议先把其中一门语言的版本读三遍、合上默写一遍，再换另一门印证——语言只是外壳，栈的动作一模一样：

\`\`\`
// JavaScript：每日温度（LeetCode 739）
function dailyTemperatures(t) {
  const ans = new Array(t.length).fill(0);   // 答案数组，默认 0（等不到更热的天）
  const stack = [];                          // 单调栈：存还在等答案的下标，温度从底到顶递减
  for (let i = 0; i < t.length; i++) {       // 一天一天往后看
    while (stack.length > 0 && t[i] > t[stack[stack.length - 1]]) {
      // 今天比栈顶那天热 → 栈顶那天终于等到答案了
      const j = stack.pop();                 //   把那天从栈里请出去
      ans[j] = i - j;                        //   等的天数 = 今天下标 - 那天下标
    }
    stack.push(i);                           // 今天还没等到答案，进栈等
  }
  return ans;                                // 留在栈里的下标，答案保持 0
}
\`\`\`

\`\`\`
# Python：每日温度（LeetCode 739）
def dailyTemperatures(t):
    ans = [0] * len(t)               # 答案数组，默认 0（等不到更热的天）
    stack = []                       # 单调栈：存还在等答案的下标，温度从底到顶递减
    for i, x in enumerate(t):        # 一天一天往后看，x 是今天的温度
        while stack and x > t[stack[-1]]:
            # 今天比栈顶那天热 → 栈顶那天终于等到答案了
            j = stack.pop()          #   把那天从栈里请出去
            ans[j] = i - j           #   等的天数 = 今天下标 - 那天下标
        stack.append(i)              # 今天还没等到答案，进栈等
    return ans                       # 留在栈里的下标，答案保持 0
\`\`\`

\`\`\`
// C++：每日温度（LeetCode 739）
vector<int> dailyTemperatures(vector<int>& t) {
  vector<int> ans(t.size(), 0);            // 答案数组，默认 0（等不到更热的天）
  vector<int> stack;                       // 单调栈：存还在等答案的下标，温度从底到顶递减
  for (int i = 0; i < (int)t.size(); i++) {  // 一天一天往后看
    while (!stack.empty() && t[i] > t[stack.back()]) {
      // 今天比栈顶那天热 → 栈顶那天终于等到答案了
      int j = stack.back();                //   栈顶那天就是等到答案的人
      stack.pop_back();                    //   把它从栈里请出去
      ans[j] = i - j;                      //   等的天数 = 今天下标 - 那天下标
    }
    stack.push_back(i);                    // 今天还没等到答案，进栈等
  }
  return ans;                              // 留在栈里的下标，答案保持 0
}
\`\`\`

> 注意：「找下一个更大 / 更小元素」的变种只有四种组合——找**左边还是右边**（决定从哪头开始遍历）× 找**更大还是更小**（决定弹出条件是 \`>\` 还是 \`<\`）。模板不用背四份，遇到变种时把遍历方向和比较符对调即可。

### 亲手跑一跑

同一份逻辑的两个语言版本，把每次弹出和每轮后的栈都打印出来——你可以亲眼看着第 3 天批量结算前两天：

\`\`\`run-js#monotonic-stack-walkthrough
const t = [30, 40, 35, 50];
const ans = new Array(t.length).fill(0);
const stack = [];                    // 存下标，温度从底到顶递减
for (let i = 0; i < t.length; i++) {
  while (stack.length && t[i] > t[stack[stack.length - 1]]) {
    const j = stack.pop();           // 第 j 天等到答案了
    ans[j] = i - j;
    console.log('第' + i + '天(' + t[i] + '度) 比第' + j + '天(' + t[j] + '度)热 -> 第' + j + '天要等 ' + ans[j] + ' 天');
  }
  stack.push(i);
  console.log('第' + i + '天处理完，栈内下标：[' + stack.join(', ') + ']');
}
console.log('答案：[' + ans.join(', ') + ']');
\`\`\`

\`\`\`run-py#monotonic-stack-walkthrough
t = [30, 40, 35, 50]
ans = [0] * len(t)
stack = []                    # 存下标，温度从底到顶递减
for i, x in enumerate(t):
    while stack and x > t[stack[-1]]:
        j = stack.pop()       # 第 j 天等到答案了
        ans[j] = i - j
        print(f'第{i}天({x}度) 比第{j}天({t[j]}度)热 -> 第{j}天要等 {ans[j]} 天')
    stack.append(i)
    print(f'第{i}天处理完，栈内下标：{stack}')
print('答案：' + str(ans))
\`\`\`

\`\`\`run-cpp#monotonic-stack-walkthrough
#include <iostream>
#include <vector>
#include <string>
using namespace std;

string fmt(const vector<int>& v) {  // 把数组格式化成 "[1, 2]" 的样子打印
  string s = "[";
  for (int i = 0; i < (int)v.size(); i++) {
    if (i > 0) s += ", ";
    s += to_string(v[i]);
  }
  return s + "]";
}

int main() {
  vector<int> t = {30, 40, 35, 50};
  vector<int> ans(t.size(), 0);
  vector<int> stack;                // 存下标，温度从底到顶递减
  for (int i = 0; i < (int)t.size(); i++) {
    while (!stack.empty() && t[i] > t[stack.back()]) {
      int j = stack.back();         // 第 j 天等到答案了
      stack.pop_back();
      ans[j] = i - j;
      cout << "第" << i << "天(" << t[i] << "度) 比第" << j << "天(" << t[j] << "度)热 -> 第" << j << "天要等 " << ans[j] << " 天" << "\\n";
    }
    stack.push_back(i);
    cout << "第" << i << "天处理完，栈内下标：" << fmt(stack) << "\\n";
  }
  cout << "答案：" << fmt(ans) << "\\n";
  return 0;
}
\`\`\`

预期输出（三个版本完全一致）：

\`\`\`
第0天处理完，栈内下标：[0]
第1天(40度) 比第0天(30度)热 -> 第0天要等 1 天
第1天处理完，栈内下标：[1]
第2天处理完，栈内下标：[1, 2]
第3天(50度) 比第2天(35度)热 -> 第2天要等 1 天
第3天(50度) 比第1天(40度)热 -> 第1天要等 2 天
第3天处理完，栈内下标：[3]
答案：[1, 2, 1, 0]
\`\`\`

读输出时盯两点：第 1 天（40 度）一进来就弹掉了第 0 天、把栈从 \`[0]\` 换成 \`[1]\`；第 3 天更猛，输出里连续两行「比第 j 天热」，把栈里攒下的 \`[1, 2]\` 两个「等待者」一口气结清——「批量结算」就发生在这里。

### 直觉想法错在哪

拿到每日温度，最自然的想法是：**对每一天，往右一个一个找，找到第一个更热的天就停下**。

这个做法完全正确——就是太慢。最坏情况温度一路降、最后一天才升高（比如 \`[50, 49, 48, ..., 51]\`）：第 0 天要向右扫 10 万天才能找到答案，第 1 天再扫 99999 天，第 2 天扫 99998 天……每一天最坏扫 n 次，总共 **O(n²)**。n = 10⁵ 就是约 50 亿次（5×10⁹）比较，竞赛和机试的 1~2 秒时限内根本跑不完，铁定超时。

再退一步看这个做法为什么「感觉对」：人眼在数组上扫一眼就能找到答案，是因为眼睛是并行扫的；代码只能一格一格看，人眼的「扫一眼」翻译成代码就是那个内层循环。所以这份直觉没错，只是要么交给计算机做就得换一种攒信息的姿势。

它慢的根源是**重复劳动**：第 1 天比第 0 天热这个「事实」，暴力做法在为第 0 天找答案时用过一次就忘了；等帮后面每一天找答案时，又把第 1 天重新翻出来比一遍。单调栈改的正是这一点——栈替我们**记住了所有还没等到答案的日子**，更热的天一来，一次性批量结算，绝不重复比较。每个下标最多进栈一次、出栈一次，**总时间 O(n)**。一句话总结：**被淘汰的元素趁早扔掉，留着只会反复干扰判断**。

> 重点：单调栈的本质再压缩成一句——栈里留的全是「还没等到答案的人」，而且按温度从栈底到栈顶递减排队。新元素来，就是按规矩给队伍清人：比你弱的全请走，你排进队尾。

### 常见错误

- **\`while\` 忘记先判栈非空**：空栈时 JS 读到 \`undefined\` 比较出错、Python 直接越界报错。先写 \`stack.length > 0\` / \`stack\` 再写比较，顺序还不能反
- **单调栈里存值不存下标**：739 要算「等几天」（下标差），84 要算宽度，存了温度就再也算不回位置了。容易犯是因为直觉上「我关心的是温度」，但温度随时能用下标去数组里取，反过来不行
- **弹出条件的 \`>\` / \`>=\` 随手写**：决定「相等的那天算不算更热」。739 的答案要「温度**更高**」，所以只有 \`>\` 才弹；写 \`>=\` 会把等温的天也算成已等到，答案偏小
- **JS 用 \`shift()\` 当队列出队**：语义对、性能错——每次 O(n)，BFS 里用它会整体退化成 O(n²)，数据一大就超时，且错误很隐蔽不容易想到。改用 \`push\` + 头指针 \`head++\`
- **括号匹配只数左右数量相等**：「\`([)]\`」左右括号各两个但根本不合法。这类题必须用栈，左括号压栈、右括号来时检查栈顶是否配对——这就是为什么 20 是栈的入门题

把上面五条串起来看，会发现它们有一个共同点：**全是「边界」问题**——空栈、相等、过期、越界。写栈 / 队列代码时，动手前先在脑子里过一遍「空的时候怎么办」「相等的时候算谁的」，能避开这个家族的大多数 bug。

### 什么时候用

- **栈**：发现「最近出现的要最先处理」——括号匹配（20）、嵌套结构展开（394 字符串解码）；还有个更深的洞察：**递归本质是系统帮你维护的调用栈**，所以 DFS、树的遍历都能用一个显式栈改写成迭代写法
- **队列**：「先访问的先扩展」——一切按层扩散的 **BFS**：二叉树层序遍历（102）、腐烂的橘子（994）。队列保证了「同一层的节点处理完，才轮到下一层」
- **单调栈**：题面能翻译成「对每个元素，找它左边 / 右边第一个比它大（或小）的元素」——739（每日温度）、84（柱状图最大矩形）、42（接雨水）
- **单调队列**（队首永远是当前区间最值的双端队列）：和滑动窗口搭配求窗口内最值——239。机制三句话：新元素进窗口前，把队尾比它小的都弹掉（它们再没机会当最大值）；窗口右移后，把已经滑出窗口的队首弹掉；剩下的队首就是当前窗口最大值。想深入可以配合《滑动窗口》一起吃透 239

一个总判断技巧：问自己「**这个信息以后还有用吗？**」——没用就趁早弹出，单调栈 / 单调队列都是这个念头的产物。

再补一个二选一的经验口诀：需求是「撤销最近的一步 / 处理最里层」就用栈，是「保持先来后到 / 一层层向外扩散」就用队列；需求里同时出现「最值」和「顺序、下一个」这类字眼，多半就是单调栈 / 单调队列的题目了。

### 练习路径

- **20 有效的括号**：建立「最近的最先处理」这个栈直觉的最佳入门，做完试着用表格法把 \`"([)]"\` 推一遍
- **155 最小栈**：学会在栈上附带额外信息（每个位置同时记住当前最小值），是「设计类」题目的敲门砖
- **739 每日温度**：本文例子，把单调栈模板默写到熟，再交换比较符写一遍「找更冷」版本
- **239 滑动窗口最大值**：单调队列应用，建议先读《滑动窗口》再来
- **84 柱状图中最大的矩形**：单调栈进阶，体会「左右两边各找一次最近的更矮柱子」如何拼出答案
- **394 字符串解码**：栈处理嵌套结构的综合小题，做完这题可以说栈真的入门了
`,
};
