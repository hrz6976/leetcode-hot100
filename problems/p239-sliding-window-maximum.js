// 239. 滑动窗口最大值
// 普通数组题：不需要 argSpec / resultKind
export default {
  id: 239,
  title: '滑动窗口最大值',
  slug: 'sliding-window-maximum',
  difficulty: 'hard',
  tags: ['队列', '数组', '滑动窗口', '单调队列'],
  hints: [
    '若一个较早元素不大于新进入窗口的元素，它会更早离开且不可能再成为最大值；只需保留未被这种关系淘汰的候选下标。',
    '维护对应值从队首到队尾单调递减的双端队列，队首始终是当前窗口最大值的下标，每个下标至多进出一次，整体 O(n)。',
    '遍历 i 时先从队尾弹出所有 nums[dq尾] <= nums[i] 的下标，再压入 i；若队首 <= i-k 则移除，i >= k-1 时记录 nums[dq首]。',
    '队列必须存下标才能判断元素是否过期，JavaScript 可用 head 指针代替 shift；ACM 输入依次为 n、数组、k，输出 n-k+1 个空格分隔的最大值。',
  ],

  description: `
给你一个整数数组 \`nums\`，有一个大小为 \`k\` 的滑动窗口从数组的最左侧移动到最右侧。你只可以看到在滑动窗口内的 \`k\` 个数字。滑动窗口每次只向右移动一位。

返回滑动窗口中的最大值组成的数组（共 \`nums.length - k + 1\` 个）。

### 示例

- 输入：\`nums = [1,3,-1,-3,5,3,6,7], k = 3\`，输出：\`[3,3,5,5,6,7]\`
- 输入：\`nums = [1], k = 1\`，输出：\`[1]\`
- 输入：\`nums = [1,-1], k = 1\`，输出：\`[1,-1]\`
- 输入：\`nums = [9,11], k = 2\`，输出：\`[11]\`

### 提示

- \`1 <= nums.length <= 10^5\`
- \`-10^4 <= nums[i] <= 10^4\`
- \`1 <= k <= nums.length\`

### ACM 模式输入输出格式

- 输入：第一行为整数 \`n\`（数组长度）；第二行为 \`n\` 个整数（空格分隔）；第三行为整数 \`k\`（窗口大小）
- 输出：各窗口的最大值（空格分隔，共 \`n - k + 1\` 个）

ACM 输入示例：
\`\`\`
8
1 3 -1 -3 5 3 6 7
3
\`\`\`
输出：\`3 3 5 5 6 7\`
`,

  functionName: 'maxSlidingWindow',
  compare: 'exact',

  tests: [
    { args: [[1, 3, -1, -3, 5, 3, 6, 7], 3], expected: [3, 3, 5, 5, 6, 7] },
    { args: [[1], 1], expected: [1] },
    { args: [[1, -1], 1], expected: [1, -1] },
    { args: [[9, 11], 2], expected: [11] },
    { args: [[2, 2, 2, 2], 2], expected: [2, 2, 2] },
    { args: [[5, 4, 3, 2, 1], 3], expected: [5, 4, 3] },
    { args: [[3, 1, 4, 1, 5, 9, 2, 6], 1], expected: [3, 1, 4, 1, 5, 9, 2, 6] },
    { args: [[3, 1, 4, 1, 5], 5], expected: [5] },
    { args: [[7, 7, 7, 7, 7, 7], 4], expected: [7, 7, 7] },
    { args: [[934,631,592,-530,-416,180,-764,990,312,-132,491,659,424,222,823,-35,-291,431,332,180,-279,226,-864,751,-354,-22,668,251,-963,903,709,-888,-325,-778,-791,100,944,-622,-487,190,64,-630,878,-95,-766,292,628,368,865,7,256,-443,829,675,-981,665,535,982,-324,-262,-387,63,-283,-111,370,298,-334,-840,411,-309,-192,-978,452,370,57,-841,887,-158,234,101,496,-83,-269,109,-830,-418,-416,-835,-206,-274,-309,-241,-258,-107,-789,88,976,-768,-279,736,-958,-57,-559,94,954,3,391,-735,-380,-447,202,-213,338,-823,504,-836,528,363,103,-779,-448,-724,585,316,376,909,836,-579,-885,350,608,808,358,918,640,202,-53,-186,1,134,-836,226,-603,318,
**
 * @param {number[]} nums
 * @param {number} k
 * @return {number[]}
 */
var maxSlidingWindow = function(nums, k) {
    
};
`,
      python: `def maxSlidingWindow(nums, k):
    # 返回每个窗口的最大值组成的列表
    pass
`,
      cpp: `#include <vector>
#include <deque>
using namespace std;

// 返回每个窗口的最大值组成的数组
vector<int> maxSlidingWindow(vector<int>& nums, int k) {
    // TODO: 在这里实现
    return {};
}
`,
    },
    acm: {
      javascript: `// ACM 模式：input 是全部输入（字符串），用 console.log 输出答案
// 输入格式：第一行 n，第二行 n 个整数，第三行 k
const lines = input.trim().split('\\n');
const nums = lines[1].trim().split(/\\s+/).map(Number);
const k = Number(lines[2]);

// 在这里写你的代码，用 console.log 输出各窗口最大值（空格分隔）

`,
      python: `# ACM 模式：用 input() 或 sys.stdin 读输入，用 print 输出答案
# 输入格式：第一行 n，第二行 n 个整数，第三行 k
import sys

lines = sys.stdin.read().split('\\n')
nums = list(map(int, lines[1].split()))
k = int(lines[2])

# 在这里写你的代码，用 print 输出各窗口最大值（空格分隔）
`,
      cpp: `// ACM 模式：用 cin 读输入，用 cout 输出答案
// 输入格式：第一行 n，第二行 n 个整数，第三行 k
#include <iostream>
#include <vector>
using namespace std;

int main() {
    int n;
    cin >> n;
    vector<int> nums(n);
    for (int i = 0; i < n; i++) cin >> nums[i];
    int k;
    cin >> k;

    // 在这里写你的代码，用 cout 输出各窗口最大值（空格分隔）
    return 0;
}
`,
    },
  },

  solutions: {
    core: {
      javascript: `var maxSlidingWindow = function(nums, k) {
  const dq = []; // 双端队列：存下标，对应值从队首到队尾单调递减
  let head = 0;  // 队首指针，用下标前移代替 shift，保证 O(1) 出队
  const ans = [];
  for (let i = 0; i < nums.length; i++) {
    // 队尾所有比 nums[i] 小的元素都不可能再成为最大值，弹出
    while (dq.length > head && nums[dq[dq.length - 1]] <= nums[i]) dq.pop();
    dq.push(i);
    // 队首下标滑出窗口 [i-k+1, i] 时出队
    if (dq[head] <= i - k) head++;
    // 窗口形成后，队首就是当前窗口最大值
    if (i >= k - 1) ans.push(nums[dq[head]]);
  }
  return ans;
};
`,
      python: `from collections import deque

def maxSlidingWindow(nums, k):
    dq = deque()  # 双端队列：存下标，对应值从队首到队尾单调递减
    ans = []
    for i, x in enumerate(nums):
        # 队尾所有比 x 小的元素都不可能再成为最大值，弹出
        while dq and nums[dq[-1]] <= x:
            dq.pop()
        dq.append(i)
        # 队首下标滑出窗口 [i-k+1, i] 时出队
        if dq[0] <= i - k:
            dq.popleft()
        # 窗口形成后，队首就是当前窗口最大值
        if i >= k - 1:
            ans.append(nums[dq[0]])
    return ans
`,
      cpp: `#include <vector>
#include <deque>
using namespace std;

vector<int> maxSlidingWindow(vector<int>& nums, int k) {
    deque<int> dq;   // 双端队列：存下标，对应值从队首到队尾单调递减
    vector<int> ans;
    for (int i = 0; i < (int)nums.size(); i++) {
        // 队尾所有比 nums[i] 小的元素都不可能再成为最大值，弹出
        while (!dq.empty() && nums[dq.back()] <= nums[i]) dq.pop_back();
        dq.push_back(i);
        // 队首下标滑出窗口 [i-k+1, i] 时出队
        if (dq.front() <= i - k) dq.pop_front();
        // 窗口形成后，队首就是当前窗口最大值
        if (i >= k - 1) ans.push_back(nums[dq.front()]);
    }
    return ans;
}
`,
    },
    acm: {
      javascript: `const lines = input.trim().split('\\n');
const nums = lines[1].trim().split(/\\s+/).map(Number);
const k = Number(lines[2]);

const dq = []; // 双端队列：存下标，对应值从队首到队尾单调递减
let head = 0;  // 队首指针，用下标前移代替 shift，保证 O(1) 出队
const ans = [];
for (let i = 0; i < nums.length; i++) {
  // 队尾所有比 nums[i] 小的元素都不可能再成为最大值，弹出
  while (dq.length > head && nums[dq[dq.length - 1]] <= nums[i]) dq.pop();
  dq.push(i);
  // 队首下标滑出窗口 [i-k+1, i] 时出队
  if (dq[head] <= i - k) head++;
  // 窗口形成后，队首就是当前窗口最大值
  if (i >= k - 1) ans.push(nums[dq[head]]);
}
console.log(ans.join(' '));
`,
      python: `import sys
from collections import deque

lines = sys.stdin.read().split('\\n')
nums = list(map(int, lines[1].split()))
k = int(lines[2])

dq = deque()  # 双端队列：存下标，对应值从队首到队尾单调递减
ans = []
for i, x in enumerate(nums):
    # 队尾所有比 x 小的元素都不可能再成为最大值，弹出
    while dq and nums[dq[-1]] <= x:
        dq.pop()
    dq.append(i)
    # 队首下标滑出窗口 [i-k+1, i] 时出队
    if dq[0] <= i - k:
        dq.popleft()
    # 窗口形成后，队首就是当前窗口最大值
    if i >= k - 1:
        ans.append(nums[dq[0]])
print(' '.join(map(str, ans)))
`,
      cpp: `#include <iostream>
#include <vector>
#include <deque>
using namespace std;

int main() {
    int n;
    cin >> n;
    vector<int> nums(n);
    for (int i = 0; i < n; i++) cin >> nums[i];
    int k;
    cin >> k;

    deque<int> dq;   // 双端队列：存下标，对应值从队首到队尾单调递减
    vector<int> ans;
    for (int i = 0; i < n; i++) {
        // 队尾所有比 nums[i] 小的元素都不可能再成为最大值，弹出
        while (!dq.empty() && nums[dq.back()] <= nums[i]) dq.pop_back();
        dq.push_back(i);
        // 队首下标滑出窗口 [i-k+1, i] 时出队
        if (dq.front() <= i - k) dq.pop_front();
        // 窗口形成后，队首就是当前窗口最大值
        if (i >= k - 1) ans.push_back(nums[dq.front()]);
    }

    for (int i = 0; i < (int)ans.size(); i++) {
        if (i) cout << ' ';
        cout << ans[i];
    }
    cout << endl;
    return 0;
}
`,
    },
  },

  idea: `
暴力法对每个窗口取最大值，时间复杂度 O(nk)，在 \`n\` 达到 \`10^5\` 时会超时。

高效解法是**单调双端队列**：维护一个「从队首到队尾对应值单调递减」的队列，窗口每右移一步：

- 新元素入队前，把队尾所有比它小的元素弹出——这些元素既比新元素小、又比新元素更早离开窗口，永远不可能再成为最大值
- 队首元素如果已经滑出窗口（下标太旧），从队首出队
- 此时队首就是当前窗口的最大值

**为什么队列里存下标而不是值**：判断「队首是否滑出窗口」必须知道元素的位置，只有下标能和当前位置 \`i\` 比较（\`下标 <= i - k\` 即已滑出）；存了下标后，取值用 \`nums[下标]\` 即可，一份信息两种用途。

每个元素最多入队、出队各一次，总时间复杂度 O(n)，空间复杂度 O(k)。
`,

  explanation: `
- \`dq\` 存的是数组下标，且始终保持 \`nums[dq[队首]] >= nums[dq[队首+1]] >= ...\` 的单调递减序
- 第一个循环 \`while (... nums[dq[dq.length - 1]] <= nums[i]) dq.pop()\`：新元素 \`nums[i]\` 入队前，把队尾所有不大于它的元素清掉，保证单调性；取等号弹出，同值时保留下标更大的新元素，反而更不容易过期
- \`if (dq[head] <= i - k) head++\`：当前窗口是 \`[i-k+1, i]\`，队首下标若小于等于 \`i - k\` 说明已在窗口外，出队。因为每轮只新增一个元素，最多只有一个队首过期，用 \`if\` 即可
- \`if (i >= k - 1) ans.push(nums[dq[head]])\`：前 \`k - 1\` 个位置窗口尚未形成，之后每步记录一次队首
- JS 版用 \`head\` 指针前移代替数组 \`shift()\`，避免每次出队都搬移整个数组；Python 版直接用 \`collections.deque\` 的 \`popleft()\`，本身就是 O(1)

ACM 版本算法完全相同，只是输入解析出 \`nums\` 与 \`k\`、最后把 \`ans\` 用空格拼接输出。
`,
};
